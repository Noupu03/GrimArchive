GrimArchive 시야-인지-반응 시스템 구현 현황 — 2026-08-05 (07문서 "다음 우선순위 1번"
E_HIT_HEAVY_INDIRECT/E_MONSTER_KILL_INDIRECT 실제 연결 + 임시 디버그 시각화 추가 반영.
이전 버전은 구현현황/지난거/시야인지반응_구현현황_2026-07-31.txt.)
기준 문서: Assets/문서/공식문서/시야인지반응/
  00_시야-인지-반응_상위구조_Current.docx.md          (v0.7, 상위 구조 — 구현 대상 아님)
  01_시야·인지범위·가시성_개념_Current.docx.md        (v0.2, 개념)
  01-A_시야·인지범위·가시성_연산공식_Current.docx.md  (v0.2, 연산공식 — 구현 대상)
  02_인지·정보판정·실패처리_시스템_v0.2.docx.md        (v0.2, 개념+연산공식 통합 — 구현 대상)
  07_전파·소리·간접입력_개념_v0.2.docx.md              (v0.2, 개념)
  07_A_전파·소리·간접입력_연산공식_v0.2.docx.md        (v0.2, 연산공식)
작성일: 2026-08-05

문서 번호 참고: 00_상위구조/02문서는 이 시스템을 "08_전파·소리"로 표기하지만 실제 폴더 파일명은
"07_..."이다(리네이밍 드리프트로 추정 — 두 표기가 가리키는 대상은 동일하다).

태그
  ✅ 구현       — 공식/수치가 코드에 정확히 있고, 실제 게임 루프에서 자동 발동.
  🔶 부분/근사  — 동작은 하지만 문서 세부 규칙 일부를 단순화했거나 근사치로 대체.
  ❌ 미구현     — 대응 게임 시스템 자체가 없어 후보/공식을 만들 수조차 없음.

════════════════════════════════════════════════════════════════════════
■ 이번 반영 내역 (2026-08-05)
════════════════════════════════════════════════════════════════════════
사용자가 07-31 구현현황 문서의 "다음 우선순위 1번"(가중치 구현현황 문서의 "다음 우선순위 4번"과
동일 작업)을 진행해 달라고 요청했다 — 07 소리 시스템 자체는 이미 완성돼 있었지만, "소리로 접근해서
확인한 결과가 실제 피격/처치 사건이었다"를 대표 가중치 시스템의 INDIRECT 이벤트로 기록하는 마지막
배선이 빠져 있었다. 이번에 그 배선 두 개를 마무리했고, 검증을 돕기 위한 임시 디버그 시각화도
함께 추가했다.

1. E_HIT_HEAVY_INDIRECT 연결 — "몬스터가 인류에게 heavyHitThreshold 이상 피해를 준" 사건을 다른
   인류가 소리로 접근해 공격자를 정확 인지하면 기록한다.
   - PropagationSystem.SoundEvent(private)에 Attacker/IsHeavyHit/IncidentId 필드 추가. EmitSound에
     동명 선택적 매개변수 추가(다른 소리 종류는 기본값 그대로 넘기면 영향 없음).
   - UnitFunction.RecordHitWeightEvent가 "인류 피해자 + 몬스터 공격자 + appliedDamage ≥
     heavyHitThreshold"를 판정해 HitImpact/HitScream 소리에 이 메타데이터를 실어 보낸다. 공격자
     정체 인지 게이팅(IsAttackerIdentified)은 피해 당사자 기준 판정이라 이 메타데이터에는 적용하지
     않는다 — 간접 확인은 전혀 다른 관찰자가 나중에 스스로 공격자를 인지해야 하는 별개 판정이기
     때문(TryConfirmIndirectHit).
   - PendingSoundReaction에 Victim/Attacker/IsHeavyHit/IncidentId 추가 — TickSoundPerception이 소리를
     PendingSound로 승격시킬 때 그대로 복사한다.
   - PropagationSystem.IsAccuratelyPerceived(observer, target) 신설 — UnitFunction.IsCurrentlyIdentified와
     동일 패턴(재판정 없이 perceptionRecords 조회만).
   - PropagationSystem.TryConfirmIndirectHit(human, alert) 신설 — TacticalFSMState.SoundAreaApproach의
     인지 판정 1회 시점(SoundPerceptionRolled이 false→true로 바뀌는 바로 그 지점)에서 호출한다. 소리
     종류가 HitImpact/HitScream이고, PendingSound에 IsHeavyHit 메타데이터가 있고, 피해자·공격자가 둘 다
     그 시점까지 생존해 있고, 관찰자가 공격자를 정확 인지했을 때만 HumanKnowledgeBase.RecordEvent를
     호출한다. incidentId는 원본 RecordHitWeightEvent가 발급한 값을 그대로 재사용해 SELF/SEEN/INDIRECT가
     같은 사건으로 묶이게 했다(6장 전역 반영 그룹화가 이 값을 키로 쓴다).
   ✅ 07문서 18장(후속 시스템 연결 표)의 이 항목이 이제 스텁이 아니라 실제 발동.

2. E_MONSTER_KILL_INDIRECT 연결 — 인류에게 죽은 몬스터의 시체를 (소리로 접근했든 우연히 지나가다
   봤든) 처음 정확 인지하면 기록한다. E_HUMAN_KILL_INDIRECT가 이미 쓰고 있던 것과 동일한 경로
   (PartyDeathSystem.OnCorpseDiscovered와 대칭 — "07 소리와는 별개의, 시체 발견 자체가 트리거인 경로")를
   몬스터 시체에도 그대로 만들었다.
   - InteractableObject에 MonsterKilledByHuman/MonsterSpeciesKey/MonsterIsSpecialUnit/
     MonsterIndividualKey 필드 추가 — 몬스터는 죽으면 Object.Destroy되어 나중에 Unit 참조로 종/개체
     키를 다시 조회할 수 없으므로(특히 target.name처럼 Unity 네이티브 프로퍼티는 Destroy 이후 접근이
     안전하지 않다), GameSession.RemoveDeadUnit이 Destroy 전(시체 InteractableObject 생성 시점)에
     HumanKnowledgeBase.ResolveTargetKey와 동일한 규칙으로 스냅샷해 둔다. u.lastAttacker가 Human일
     때만(E_MONSTER_KILL_SELF/SEEN과 동일 조건 — "인류가 처치") MonsterKilledByHuman=true.
   - HumanKnowledgeBase.RecordEventByKey(id, observer, targetKey, isIndividualTarget, infoType,
     incidentId) 신설 — target: Unit이 아니라 스냅샷 키를 직접 받는 경로. 기존 RecordEvent(target: Unit)는
     이제 ResolveTargetKey(target)을 얹어 이 메서드를 호출하는 얇은 래퍼로 리팩터링됐다(동작 동일,
     회귀 없음 — WeightSystemTests.cs에 새 테스트로 고정).
   - UnitFunction.CastRay의 기존 오브젝트 발견 파이프라인에 `objKind == Corpse && Tags.Contains
     ("Monster")` 분기 추가(Corpse+Human 분기 바로 아래, 완전히 동일한 firstTouch/AccuratePerception
     게이팅) → PropagationSystem.OnMonsterCorpseDiscovered(discoverer, corpse) 호출.
   - PropagationSystem.OnMonsterCorpseDiscovered — MonsterKilledByHuman이 아니면(야생끼리 등) 조용히
     무시, 맞으면 RecordEventByKey로 기록. incidentId는 corpse.Id를 그대로 써서, 같은 시체를 나중에
     다른 인류가 또 발견해도 같은 사건으로 그룹화된다.
   ✅ 07문서 18장 이 항목도 이제 스텁이 아니라 실제 발동.

3. [임시, 검증 보조 — 이후 4/5번에서 계속 수정됨] PropagationDebugVisualizer 신설 — ThreatTileRenderer와
   동일 컨벤션(GameObject 불필요한 순수 C# 클래스, GameSession이 매 프레임 Render(units) 호출,
   GameCompositionRoot에 Singleton 등록)으로 씬에 두 가지를 그린다: (1) 살아있는 인류마다 현재 전파
   범위(6장, 카리스마 기반)를 옅은 하늘색 원으로, (2) 소리 발생 위치에 소리 종류별 색 원. 최초 버전은
   반경이 남은 유효시간에 비례해 줄어드는 연출이었으나, 아래 4/5번에서 사용자 피드백으로 두 차례 더
   수정됐다 — 이 문서에서 "지금 실제로 어떻게 동작하는지"는 4/5번 항목을 봐야 정확하다.

4. [사용자 피드백 반영] 소리 시각화 반경 표현 수정 — "실제 소리 감지 범위가 시간이 지나며 줄어드는
   건 아니잖아, 반경이 줄어드는 연출은 오해를 준다"는 지적을 받아, 반경은 PropagationMath.
   SoundBaseRange(소리 종류별 고정값)로 그대로 두고 남은 시간은 투명도로만 표현하도록 고쳤다(반경
   고정 + 알파 페이드아웃).

5. [사용자 피드백으로 아키텍처 자체를 재설계 — 이번 반영 중 가장 큰 항목] 소리 시스템을 폴링에서
   이벤트 기반(R3+UniTask)으로 전환.
   배경: 위 4번 수정 과정에서 사용자가 "소리 유효시간 5초가 원래 이렇게 긴가?"라고 물어 07-A 7-3장
   ("일반 소리 유효시간: 발생 후 5초")을 확인시켜줬는데, 이어서 사용자가 이 5초의 실제 의도를
   정정했다 — "5초는 유닛이 소리를 감지했을 때 그 인지반응이 유효한 시간이지, 소리 자체가 5초 동안
   계속 발생 중이라는 뜻이 아니다." 그런데 기존 구현(EmitSound가 SoundEvent를 5초짜리 _activeSounds
   목록에 넣고, 모든 인류가 UnitFunction.OnUpdate의 0.1초 틱마다 TickSoundPerception으로 그 목록
   전체를 훑는 폴링 방식)은 정확히 그 반대로 동작하고 있었다 — 소리가 5초 동안 "월드에 남아있어서"
   그 사이 범위 안으로 뒤늦게 걸어 들어온 유닛도 주워들을 수 있었다. 사용자는 이걸 기획 관점에서도
   최적화 관점에서도(모든 인류가 상시 폴링) 문제로 판단해 다음 방향으로 전환을 지시했다: "범위
   스캔 후 R3 이벤트를 터뜨려 인지반응을 위한 5초를 UniTask 등으로 발동시키는 것."
   구현 (PropagationSystem.cs 대폭 재작성):
   - EmitSound(session, type, position, floorIndex, source, attacker, isHeavyHit, incidentId)가
     호출된 그 자리에서 즉시 session.units를 순회하며 범위 스캔을 끝낸다(같은 층/같은 공간(roomId)/
     CanPerceive/IsSoundUnresponsive/이동음 자기·아군 제외/SoundDetectionRange 이내 — 기존
     TickSoundPerception이 각 인류 관점에서 하던 조건 판정을 그대로, 다만 "소리쪽에서 한 번에" 수행).
   - 조건을 만족하는 인류마다 PropagationSystem.OnSoundPerceived(R3 Subject<SoundPerceivedEvent>)로
     1회 통지한다. 이 Subject는 public으로 열려 있어 디버그 시각화 등 다른 구독자도 자유롭게 붙을 수
     있다 — 핵심 게임 로직(PendingSound 갱신)은 static 생성자에서 스스로 구독해 항상 보장된다.
   - private HandleSoundPerceived(SoundPerceivedEvent)가 실제 PendingSound 갱신을 담당 — 우선순위/
     거리 비교로 "기존 유지할지 교체할지" 판단하는 로직(7-2장)은 예전 TickSoundPerception 안에 있던
     것을 그대로 옮겼다(동작 동일, 트리거 방식만 바뀜 — 같은 공격에서 공격 실행음→피격 발생 공격음→
     비명이 연속 EmitSound 호출로 이어져도 R3 Subject.OnNext가 동기 호출이라 매번 "그 시점의
     PendingSound"와 비교되므로 최종적으로 가장 급한 소리가 남는다, 예전과 동일한 결과 보장).
   - 5초 유예시간은 ExpireAfterDelay(async UniTaskVoid)가 담당 — UniTask.Delay(5초) 후 그 reaction이
     여전히 PendingSound에 남아있고 ResponseStarted가 안 됐으면 null로 비운다. 기존에도
     TryPromotePendingSoundToAlert 안에 "Time.time > ValidUntilTime이면 정리"하는 지연 체크가 있어
     사실 완전히 새 보장이 필요한 건 아니었지만(그 체크가 TacticalFSMState.HasAlert를 통해 매 틱
     계속 호출되므로 결과적으로 비슷하게 정리됨), 그 정리가 "다른 시스템이 우연히 그 체크 지점을
     다시 지나가는지"에 기대는 구조라 사용자 요청대로 감지 시점 자체를 기준으로 하는 명시적 타이머로
     교체해 더 견고하게 만들었다.
   - UnitFunction.OnUpdate에서 PropagationSystem.TickSoundPerception(human) 호출 삭제 — 이제 그
     자리에서 반복 재평가할 대상 자체가 없다(감지는 EmitSound 시점에 끝남).
   - PropagationDebugVisualizer도 함께 재작성 — DebugGetActiveSounds()(활성 소리 목록 조회)가
     사라졌으므로, 대신 OnSoundPerceived를 구독해 "방금 감지된 소리"를 받을 때마다 그 위치에 짧은
     플래시(DebugFlashSeconds=1.2초, 07-A 7-3장의 5초 유예시간과는 무관한 순수 시각화용 임의값)를
     기록해뒀다가 그 시간 동안만 표시하고 지운다. 같은 소리를 여러 인류가 동시에 감지하면 관찰자별로
     이벤트가 여러 번 오므로 같은 위치에 원이 겹쳐 그려질 수 있는데(중복 제거 안 함), 임시 시각화라
     정확한 집계보다 "여기서 방금 무슨 소리가 났는지"가 한눈에 보이는 걸 우선했다.
   범위 스캔 방식 자체에 대한 자문: 사용자가 "좌표계를 통한 수학적 계산이 유리한지 다른 방식이
   나은지" 조언을 요청해, 다음과 같이 답하고 우선 기존 방식(session.units 전체 순회 + Euclidean
   거리)을 그대로 유지했다 — 결정된 사안이 아니라 자문으로 남긴 것이라 코드는 바꾸지 않음:
     * 지금 게임 규모(웨이브당 인류 소수, 활성 소리도 순간적으로 몇 개뿐)에서는 이 정도 전체 순회
       비용이 무시할 만하다.
     * 좌표 수학(거리 계산) 자체는 어느 방식을 쓰든 최종 판정에 필요하다 — 공간 분할/인덱싱은
       "스캔 후보를 줄이는" 역할이지 거리 계산 자체를 대체하지 않는다.
     * 유닛 수가 늘어나 스캔 비용이 문제가 되면, CreateMap이 이미 갖고 있는 roomId를 키로 하는
       "방별 인류 후보 인덱스"(Dictionary<int, List<Human>>, 유닛이 방을 옮길 때 갱신)를 추가해
       EmitSound가 그 소리가 발생한 방(과 필요시 인접 통로)에 있는 인류만 순회하도록 좁히는 게
       다음 단계로 자연스럽다 — GameSession이 이미 방/유닛 위치를 추적하는 인프라(roomGrid,
       RegisterUnitPos 등)가 있어 그 위에 얹기 쉽다. 쿼드트리/공간 해시 같은 범용 공간 분할은 지금
       규모(작은 웨이브 단위 던전, 유닛 수 적음)에는 과한 최적화로 판단해 추천하지 않았다.
   테스트: PropagationSystemTests.cs에 OnSoundPerceived_SetsPendingSound_AndRespectsPriority 신규 —
   이벤트를 직접 발행해 PendingSound가 세팅되는지, 우선순위 낮은 소리가 기존 소리를 못 덮어쓰는지,
   더 급한 소리는 덮어쓰는지 고정. EmitSound 자체의 범위 스캔은 GameSession/CreateMap 전체 셋업이
   필요해 이 테스트 범위 밖 — 실제 플레이 검증 항목으로 아래에 남긴다.

6. [사용자 요청] DebugInfoPanel에 "소리/전파 표시" 토글 추가 — 기존 "시야 표시" 토글
   (UnitGenerate.ShowAllVisionRanges, DebugInfoPanel.DrawVisionToggle)과 완전히 동일한 컨벤션으로
   우측 상단에 버튼을 하나 더 그린다(그 바로 아래, Rect(Screen.width-220, 55, 200, 40)). 기본값은
   꺼짐 — PropagationDebugVisualizer.Enabled를 토글한다.

7. [버그 수정] 버튼을 켜도 원이 전혀 안 보인다는 신고 — 렌더러를 LineRenderer에서 SpriteRenderer로
   교체. 최초 구현은 LineRenderer + `new Material(Shader.Find("Sprites/Default"))`로 원을 그렸는데,
   이 프로젝트가 URP를 쓰고 있어(ProjectSettings/GraphicsSettings.asset 확인) 그렇게 즉석으로 만든
   머티리얼이 정상 렌더링된다는 보장이 없었다(실제로 안 보였음 — 검증된 적 없는 경로였다). 대신
   ThreatTileRenderer가 이미 증명한 방식(SpriteRenderer + 스프라이트, 머티리얼을 직접 안 건드리고
   SpriteRenderer 기본 스프라이트 머티리얼에 맡김)을 그대로 따라, 원형 링 텍스처를 코드로 한 번
   절차적으로 생성(Texture2D 픽셀 채우기 → Sprite.Create, 128px, 링 두께 outerR 0.95~innerR 0.75로
   충분히 두껍게)해 재사용하는 방식으로 교체했다. transform.localScale로 반지름을 조절한다(스프라이트
   자체는 로컬 반지름 1로 만들어 scale=반지름 매핑을 단순하게 유지). 사망음 색도 검정(어두운 바닥
   위에서 안 보일 수 있음)에서 흰색으로 바꿨다.

════════════════════════════════════════════════════════════════════════
■ 07문서 장별 구현 현황 (07-31 대비 변경분만 — 나머지는 지난거/시야인지반응_구현현황_2026-07-31.txt
  그대로 유효)
════════════════════════════════════════════════════════════════════════
✅ 18장(후속 시스템 연결 표) — E_HIT_HEAVY_INDIRECT/E_MONSTER_KILL_INDIRECT 두 항목이 위 "이번 반영
   내역" 1/2번으로 실제 발동한다. 남은 미연결 항목(합류 5초 초과 처리/코어 물리적 전파/10장 상호작용
   인스턴스 구분/전투 중 소리 새 타겟 탐색/함정 수치/몬스터 무리형 전파)은 07-31본과 동일하게 아직
   후속 문서(08/09/06/함정) 부재로 스텁.

════════════════════════════════════════════════════════════════════════
■ 검증 상태
════════════════════════════════════════════════════════════════════════
- 컴파일: `dotnet build Assembly-CSharp.csproj`로 전체 빌드 성공 확인(오류 0, 기존 경고 39개와 동일 —
  4/5/6번(소리 재설계+시각화 수정+UI 토글) 포함해서 새로 생긴 경고 없음). Assembly-CSharp.csproj는
  gitignore 대상이라 이번에 추가한 PropagationDebugVisualizer.cs의 Compile 항목은 지금은 수동으로
  넣어뒀고, Unity가 다음에 프로젝트를 열 때 재생성되며 자동으로 유지된다.
- 테스트: WeightSystemTests.cs에 RecordEventByKey_AppliesSamePersonalDeltaAsRecordEvent 1개,
  PropagationSystemTests.cs에 TryConfirmIndirectHit_RecordsOnlyWhenAttackerAccuratelyPerceived +
  OnMonsterCorpseDiscovered_OnlyRecordsWhenKilledByHuman + OnSoundPerceived_SetsPendingSound_
  AndRespectsPriority(5번 재설계 검증 — 이벤트 직접 발행으로 PendingSound 세팅/우선순위 유지·교체
  확인) 3개, 총 4개 신규 — 전부 dotnet build로 컴파일 확인 + 손으로 재계산해 기대값과 대조. 기존
  07/07-A 테스트(20여 개)와 마찬가지로 Unity Test Runner로 실제 실행은 아직 안 했다(에디터 필요) —
  다음 세션 최우선 확인 대상 항목에 이번 신규 4개도 포함.
- 실제 플레이 검증: 전무 — 07-31본이 남긴 4개 항목(합류 대기/소리 반응/보호 포메이션 참여/코어·함정·
  사망 정보 공유 지연 전달) 그대로 유효하고, 이번에 추가로 확인이 필요한 항목:
  5. E_HIT_HEAVY_INDIRECT — 몬스터에게 얻어맞는 소리를 다른 인류가 듣고 접근해 공격자를 정확
     인지했을 때 실제로 위험도/이해도가 오르는지, 공격자를 못 알아본 채로 확인 시간이 끝나면
     (EstimatedAreaHoldSeconds 경과) 기록 없이 조용히 종료되는지.
  6. E_MONSTER_KILL_INDIRECT — 인류가 처치한 몬스터 시체를 나중에 다른 인류가 발견하면 이해도가
     오르고 위험도가 완화되는지, 야생 몬스터끼리 죽인 시체는 발견해도 아무 반응이 없는지.
  7. [5번 재설계로 성격이 바뀐 항목] 소리 감지 — 예전엔 "범위 안에 들어오면 나중에라도 반응하는지"가
     확인 대상이었는데, 이제는 반대로 "소리가 발생한 그 순간 범위 밖에 있던 인류가 나중에 그 자리로
     들어와도 반응하지 않는지"(의도된 동작), 그리고 "발생 순간 범위 안에 있던 인류는 즉시 반응하고
     ExpireAfterDelay로 5초 뒤 정확히 정리되는지(다른 더 급한 소리로 안 바뀐 채 5초가 지나면
     PendingSound가 비는지)"가 확인 대상이다.
  8. PropagationDebugVisualizer(임시, DebugInfoPanel "소리/전파 표시" 토글로 켬) — 전파 범위 원이
     실제로 씬에 그려지는지, 소리를 실제로 감지한 순간에만 짧게(1.2초) 플래시가 뜨는지(예전처럼
     5초씩 오래 안 남는지), 다층 구조 던전에서 현재 보고 있는 층이 아닌 다른 층의 소리도 같이
     그려져 혼동을 줄 수 있다는 한계(임시 시각화라 층 필터링은 넣지 않았음)를 사용자가 인지하고
     있는지.
  9. 방별 인류 후보 인덱스(9번) — 방을 옮겨도(계단/워프 포함) 그 인류가 새 방에서 소리를 정상적으로
     듣는지(인덱스 갱신 누락으로 "투명 인간"이 되지 않는지), 죽은 인류가 인덱스에서 실제로 빠지는지
     (메모리 누수·유령 감지 방지), 파티 여러 개가 서로 다른 방에서 동시에 활동할 때 다른 방의 소리에
     반응하지 않는지(공간 판정 자체는 원래도 있었지만 인덱스가 그 경계를 흐트러뜨리지 않는지 재확인).
  10. [2026-08-06 신규] PropagationSystem.PropagateAttackedFact(7-2장) — 파티원이 공격자를 못 알아본
     채 맞았을 때 같은 방·전파 범위 안의 비전투 다른 파티원이 실제로 미식별 공격 수색 상태로 전환되는지,
     범위 밖·다른 방 파티원은 반응하지 않는지, 이미 다른 경계/조사/전투 중인 파티원은 상태가 안
     덮어써지는지.

════════════════════════════════════════════════════════════════════════
■ 다음 우선순위 구현 후보 (2026-08-05, 07-31본에서 1번이 완료돼 빠지고 번호가 당겨짐)
════════════════════════════════════════════════════════════════════════
1. [작음] 07-A 11장 "실제 합류 최대 대기 5초 초과 후 행동" — 09_목표·이동경로 문서 부재로 지금은
   안전한 기본값(단독 전투 시작)으로 스텁. 09문서가 생기면 재검토.
2. [작음] 코어 발견 물리적 전파(발견자→리더 실제 이동) — 09문서 부재로 TickLeaderPropagation의
   "우연히 범위 안에 들어오면 전달" 근사로 대체 중.
3. [작음] 10장 NotifiedActiveInteractions의 상호작용 인스턴스 구분 — 지금은 유닛 단위 영구 기록이라
   드물게 오래된 전파 기록으로 잘못 참여자격을 얻는 경우가 있을 수 있음.
4. [큼, 문서 부재로 보류] 06_전투반응·기습 문서가 생기면: 16-5장 "전투 중 소리를 새 타겟 탐색 입력으로
   사용" 연결(전투 타겟 변경 시스템 자체가 없어 현재 스텁), 15장 반응 후보 실제 선택 로직.
5. [큼, 문서 부재로 보류] 08/09문서가 생기면: 7-3장 전투 합류 시 정보 전달, 09-A 11장 5초 초과 처리,
   09장 정식 파티 통신 체계로 지금의 즉석 판정(StartJoinCombatWait 등)을 대체.
6. [큼, 문서 부재로 보류] 함정 시스템 문서가 생기면: 함정 피해 인원수·단계별 위험도 증가량, 함정
   발동 흔적 이해도 상승량(1회/최대) — 07-A 11장이 "미정"으로 명시. 함정작동음 소리 이벤트 자체는
   이미 발생하므로, 값만 정해지면 바로 연결 가능.
7. [작음] 몬스터 무리형 전파/재전파, 몬스터 소리 감지 실제 행동 반응 — 대응 "역할군 AI" 시스템 부재로
   스텁(PropagationMath.SoundDetectionRange(isMonster:true)는 값만 준비됨).
8. [정리, 급하지 않음] PropagationDebugVisualizer(임시 시각화) — 위 "실제 플레이 검증" 항목 5/6번
   확인이 끝나면 필요 없어지는 코드다. 계속 유지할지, 검증 후 지울지 사용자 판단 필요(파일 자체가
   "임시" 용도임을 주석에 명시해 뒀으니 판단 근거는 남아있음).
9. [완료, 2026-08-05] EmitSound 범위 스캔 최적화 — 사용자가 자문(9번 원안) 내용을 그대로 적용하기로
   결정해 구현까지 끝났다. PropagationSystem에 (Floor, RoomId) 튜플을 키로 하는
   Dictionary<(int,int), HashSet<Human>> _humansByRoom + 역방향 Dictionary<Human,(int,int)>
   _humanRoomKey를 추가(UpdateHumanRoomIndex/RemoveFromRoomIndex). roomId는 층마다 번호가 재사용될
   수 있어(CreateMap.GetRoomOccupationState가 floorIndex를 항상 별도로 요구하는 것과 동일 이유)
   튜플 키를 썼다. 유지보수는 새 폴링을 만들지 않고 기존 유닛 그리드 관리 지점(GameSession.
   RegisterUnitPos/UnregisterUnitPos — 스폰/이동/사망 시 이미 호출되는 곳, WaveSpawner/
   HumanWaveManager/NavigationFSMState/DebugInputHandler 등 모든 인류 스폰·이동·제거 경로가 이
   두 래퍼를 거치는 걸 grep으로 확인)에 얹었다. EmitSound는 이제 session.units 전체 대신
   _humansByRoom[(floorIndex, roomId)]만 순회한다(최종 거리 판정은 자문대로 여전히 Vector2Int.
   Distance 좌표 수학 — 공간 분할은 후보를 줄이는 역할이지 거리 계산을 대체하지 않는다는 원칙 유지).
   순회 중 인덱스 변경 가능성에 대비해 재사용 버퍼(_scanBuffer)로 스냅샷 후 순회. 테스트: 이 인덱스
   내부 상태를 직접 조회하는 public API가 없어(private 필드) 전용 단위 테스트는 추가하지 않았다 —
   EmitSound 자체가 GameSession 풀 셋업이 필요해 애초에 플레이 검증 항목이었던 것과 동일 사유,
   아래 "실제 플레이 검증"에 추가.
10. [완료, 2026-08-06] 07-A 7-2장 동순위 소리 타이브레이크 3단계 중 "거리도 같으면 현재 시야 방향과
    가까운 쪽 우선" — PropagationMath.ShouldReplaceSound에 방향 인자 추가로 해결(위 "2026-08-06 갭
    해결" 1번 참고).
11. [완료, 2026-08-06] PropagationComponent.PendingAttackDir 죽은 필드 — 완전 삭제(위 "2026-08-06 갭
    해결" 2번 참고).
12. [완료, 2026-08-06] 07 문서 7-2장 "공격받았지만 미인지" 시 다른 유닛에게로의 실제 브로드캐스트 —
    PropagationSystem.PropagateAttackedFact 신설로 해결(위 "2026-08-06 새 이슈 해결" 참고).

════════════════════════════════════════════════════════════════════════
■ 사용자 확인이 필요했던 판단
════════════════════════════════════════════════════════════════════════
- 이번 작업(E_HIT_HEAVY_INDIRECT/E_MONSTER_KILL_INDIRECT 연결)은 CLAUDE.md 관례상 확인이 필요할 수
  있는 설계 결정 두 개를 포함했지만, 둘 다 기존 코드베이스의 이미 확립된 전례를 그대로 따르는
  선택이라 별도로 묻지 않고 진행했다 — 판단 근거만 남긴다.
  1. "간접 확인 시점을 언제로 볼 것인가" — 07-A 8-2장 원문이 "인지 판정 1회, 원인을 정확 인지하면
     다른 경로가 우선권을 가져간다"고만 말하고 세부 로직은 안 준다. E_HUMAN_KILL_INDIRECT가 이미
     "시체 발견(오브젝트 인지 파이프라인)"을 트리거로 쓰고 있던 전례를 그대로 따라 E_MONSTER_KILL_
     INDIRECT도 동일 경로(Corpse 발견)로, E_HIT_HEAVY_INDIRECT는 대응할 영속 오브젝트가 없어(피격은
     시체를 안 남긴다) SoundAreaApproach의 인지 판정 시점을 직접 훅했다.
  2. "죽은 몬스터의 종/개체 키를 어떻게 안전하게 들고 있을 것인가" — PartyDeathRecord가 이미
     "Destroy될 대상의 정보는 스냅샷으로 미리 떠 둔다"는 패턴을 쓰고 있어(DeadUnitName 등 문자열
     필드) 그대로 InteractableObject에 스냅샷 필드를 추가하는 동일 패턴을 적용했다.

- 위 4/5/6번(소리 시각화 반경 수정 → 소리 시스템 이벤트 기반 재설계 → UI 토글)은 전부 사용자가 직접
  구체적으로 지시한 방향이라 확인 절차 없이 그대로 구현했다 — 다만 5번 안에서 "범위 스캔 방식 자체"는
  사용자가 명시적으로 "아직 고려 안 함, 조언해달라"고 선을 그은 별개 사안이라, 코드는 바꾸지 않고
  자문만 제공했다(위 "다음 우선순위" 9번에 자문 내용 기록). 사용자가 방별 인덱스 적용을 원하면
  그때 실제로 구현하면 된다.

════════════════════════════════════════════════════════════════════════
■ 2026-08-05 추가 수정 8번 (같은 날 두 번째 버그 수정)
════════════════════════════════════════════════════════════════════════
소리 플래시가 "한 프레임만에 사라지는 것 아니냐"는 사용자 지적으로 재점검해 실제 버그를 하나 더
찾았다 — RenderActiveSounds가 원을 "_recentSounds 리스트의 인덱스 위치"로 재사용 풀에 배정하고
있었다. 프레임마다 오래된 플래시가 만료되고 새 플래시가 추가되며 리스트 구성이 바뀌면, 같은 풀
슬롯(GameObject)이 순간적으로 다른 플래시의 위치·색으로 재배정될 수 있었다(원이 있어야 할 자리에서
사라지거나 다른 곳으로 튀는 것처럼 보이는 원인). 각 플래시가 발생 즉시(구독 콜백에서) 자기 전용
GameObject를 만들어 만료될 때까지 그대로 들고 있다가 Destroy하는 방식으로 교체해 이 재배정 자체를
없앴다 — 풀링 로직(_soundVisualPool)도 함께 제거. dotnet build로 컴파일 확인 완료(오류 0).

════════════════════════════════════════════════════════════════════════
■ 2026-08-05 추가 수정 9번 (임시 진단 로그 제거 + 시각화 트리거 변경)
════════════════════════════════════════════════════════════════════════
직전(8번)에 넣었던 [SoundDebug] 임시 진단 로그(EnableSoundDebugLog/DebugLogSound)는 사용자 요청으로
전부 제거했다. 대신 사용자가 원한 건 "감지 성공 여부와 무관하게, 몬스터가 소리를 내는 범위 자체가
보이는 시각화"였다 — 기존 PropagationDebugVisualizer는 OnSoundPerceived(누군가 실제로 감지에
성공했을 때만 발동)를 구독하고 있어서, 범위 밖이거나 이미 다른 걸 보고 있어서 감지가 안 되면 원 자체가
안 떴다(이게 "몬스터 이동음이 안 보임" 신고의 실제 원인이었을 가능성이 큼 — IsSoundUnresponsive/거리
등으로 거를 때마다 조용히 아무 것도 안 그려짐).

PropagationSystem에 새 이벤트 OnSoundEmitted(SoundEmittedEvent: Type/Position/FloorIndex/
RangeTiles) 신설 — EmitSound가 room/candidate 스캔 결과와 무관하게 매번 무조건 1회 발행한다(순수
디버그 시각화 전용, 게임 로직은 이 이벤트를 쓰지 않음). PropagationDebugVisualizer는 이제
OnSoundPerceived 대신 이 이벤트를 구독한다 — 이제 감지 성공 여부와 상관없이 소리가 날 때마다
그 위치에 SoundBaseRange 반경의 원이 뜬다. 다만 이 방식은 "실제로 누가 반응했는지"는 더 이상 이
시각화만으로는 알 수 없다는 한계가 생겼다(원이 뜬다고 반드시 어떤 인류가 반응했다는 뜻은 아님) —
필요하면 나중에 OnSoundPerceived 쪽 시각화(예: 감지 성공한 인류 위에 표시)를 별도로 추가하는 것도
고려 가능. dotnet build로 컴파일 확인 완료(오류 0).

════════════════════════════════════════════════════════════════════════
■ 2026-08-05 추가 수정 10번 (실제 버그 수정: 소리 반응 접근 중 15초 워치독 오적용)
════════════════════════════════════════════════════════════════════════
사용자가 "소리 반응이 여러 번 겹칠 때 다른 로직과 꼬이는 부분이 있는지" 분석을 요청해 fork로 조사한
결과, TacticalFSMState/CombatFSMState 우선순위 체계 자체는 설계대로 정확히 반영돼 있었지만(전투 중엔
소리 스캔 후보에서 아예 제외/함정 대응 중 소리 인터럽트 게이팅 실동작/JoinCombatWait 우선순위 등
전부 확인됨), 실제 버그를 하나 발견했다.

원인: AlertSearchState.ElapsedSeconds 필드를 두 군데가 서로 다른 목적으로 공유해서 썼다.
- UnitFunction.OnUpdate(당시 줄1036-1050): currentAlertSearch가 있으면 매 프레임 무조건 누적시키다가
  IsPostCombatSweep/IsDeathSearch가 아니면 03문서 4-11장 "미식별 공격 수색시간"(15초, 공격은 당했는데
  누가 때렸는지 모르는 경우 전용)을 워치독으로 적용 — IsSoundResponse 여부를 전혀 안 봄.
- TacticalFSMState.SoundAreaApproach/SoundMoveReact: 인지 판정 롤 시점에 같은 필드를 0으로 리셋해서
  "롤 이후 2초 유지"(EstimatedAreaHoldSeconds/MoveSoundHoldSeconds)용으로 재사용.

07-A 7-3장 표를 재확인한 결과 "확인 행동 시작 기한: 5초 안에 시작 / 시작 후 5초 초과: 확인 행동
계속 수행"이라고 명시돼 있어 — 소리 반응 접근 자체엔 완료 시간 제한이 없는 게 문서 의도임을 확인.
15초는 03문서 4-11장이 명시적으로 "미식별 공격 수색" 전용으로 정의한 값이라 소리 반응 접근에 적용될
근거가 없다. 실제로는 감지 범위가 멀면(spotting 보정 포함 8~12칸까지 가능) 도착 전에 이 워치독에
걸려 currentAlertSearch가 조용히 통째로 사라지는 버그였다 — SoundPerceptionRolled가 영원히
false인 채 끝나서 인지 판정 자체가 안 굴려지고, TryConfirmIndirectHit(E_HIT_HEAVY_INDIRECT 확인)
기회도 함께 날아갔다. 이동음/공격실행음/함정작동음(IsUrgentSoundApproach 아님)은 경계 이동속도
75%까지 겹쳐서 이 워치독에 걸릴 위험이 더 컸다.

수정: UnitFunction.OnUpdate에 `IsSoundResponse && !SoundPerceptionRolled`(아직 접근 중) 조건이면
15초 워치독 자체를 적용하지 않는 예외를 추가. 롤 이후엔 손댈 필요 없음 — 롤 시점에 ElapsedSeconds가
0으로 리셋되고 2초 규칙이 15초보다 항상 먼저 트리거되므로 워치독과 절대 안 겹친다.

테스트: Assets/Tests/FSMBehaviorTests.cs에 SoundResponseApproach_NotCutOffByUnidentifiedAttackWatchdog
(16초를 한 번에 흘려도 소리 반응 접근 중이면 안 잘림) + UnidentifiedAttackSearch_
StillCutOffAfter15Seconds(회귀 방지 — 원래 4-11장 워치독 자체는 그대로 작동하는지) 2개 신규.
human.OnUpdate(deltaTime)을 직접 호출하는 이 프로젝트 최초의 테스트 — SyncRoomAffiliation이
Session?.roomGrid==null 가드가 있어 Session 없이도 안전하게 호출 가능함을 확인 후 작성.
dotnet build로 컴파일 확인 완료(오류 0).

════════════════════════════════════════════════════════════════════════
■ 2026-08-05 추가 수정 11번 (UI 요청: 항목별 토글 + 상태 가시성 개선)
════════════════════════════════════════════════════════════════════════
사용자 요청 2가지: (1) 단일 "소리/전파 표시" 토글을 앞서 정리한 색 표 기준(전파 범위 + 소리 6종)별로
개별 토글 가능하게, (2) 버튼 자체가 켜짐/꺼짐 상태를 눈에 띄게 보여주도록.

PropagationDebugVisualizer: 단일 `Enabled` bool을 `ShowPropagationRange`/`ShowMovement`/
`ShowAttackExecution`/`ShowHitImpact`/`ShowHitScream`/`ShowDeath`/`ShowTrapActivation` 7개로 분리.
`IsSoundTypeEnabled(SoundType)` 스위치로 OnSoundEmitted 구독 콜백에서 해당 종류가 꺼져 있으면 아예
GameObject를 안 만들도록 필터링. `AnyEnabled`(하나라도 켜졌으면 true)로 루트 GameObject
활성화 여부를 판단 — 전파 범위만 꺼도 그 원들은 즉시 정리(Destroy)되도록 RenderPropagationRanges
앞단에 처리 추가.

DebugInfoPanel.DrawPropagationToggle: 기존 버튼 1개 → 7개 버튼(전파 범위 하늘색/이동음 회색/공격
실행음 주황/피격 발생 공격음 빨강/피격 비명 분홍/사망음 흰색/함정 작동음 노랑) 세로 나열로 교체
(Rect 55~295, y=360 "Unit Status Test" 패널과 안 겹침 확인). `DrawColorToggle` 헬퍼가
`GUI.backgroundColor`를 켜짐이면 그 항목의 실제 원 색, 꺼짐이면 회색으로 칠해서 — 텍스트만 바뀌던
예전 버튼보다 상태가 훨씬 잘 보이고, 버튼 색이 실제 원 색과 대응돼 표를 안 봐도 직관적으로 연결된다.
dotnet build로 컴파일 확인 완료(오류 0).

════════════════════════════════════════════════════════════════════════
■ 2026-08-05 추가 수정 12번 (UI 배치: 자원 현황과 겹침 해소)
════════════════════════════════════════════════════════════════════════
"소리/전파 시각화" 7토글 패널(Screen.width-220, y55~295)이 StatusInfoPanel(오펜스 현황/자원 사용
안내, x:Screen.width-270~Screen.width-10, y:50~350)과 그대로 겹친다는 지적을 받아 좌측 상단으로
옮겼다. UIManager.DrawTopLeftUI를 확인해보니 y10~100(FPS 카운터+게임 속도 표시)만 실제로 쓰고
있고 나머지 좌측 상단 UI(유물 생성 모드/파티 상태/유닛 라벨)는 전부 주석 처리돼 죽어있어 y110부터
비어있다는 걸 확인 — 그 자리(Rect(10, 110, 220, 240))로 이동. 화면 하단 좌측(x10~250)의 선택 유닛
정보 UGUI 프리팹, BuildingControlPanel(x260~520, 화면 하단)과도 세로로 충분히 떨어져 있어 겹치지
않는다. dotnet build로 컴파일 확인 완료(오류 0).

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 재검증 (구현현황_정리_20260805.txt 7개 항목 + 07/07-A 전체 항목 코드 대조)
════════════════════════════════════════════════════════════════════════
사용자가 (구본훈 공유용) 구현현황_정리_20260805.txt와 07/07-A 원본 문서를 근거로 "각 항목별로
재검증해서 정리해달라"고 요청 — fork로 실제 소스 코드를 직접 읽어 어제 기록을 독립 재확인했다(코드
수정 없음, 조사만).

**정리_20260805.txt 7개 항목 — 전부 CONFIRMED(코드와 정확히 일치).**
1. E_HIT_HEAVY_INDIRECT/E_MONSTER_KILL_INDIRECT — TryConfirmIndirectHit이 SoundAreaApproach의 인지
   판정 롤 시점에서 정확히 호출되고, RecordEventByKey/RecordEvent 래퍼 관계, RemoveDeadUnit의 Destroy
   전 스냅샷, IsAttackerIdentified와 간접 확인 판정의 분리까지 서술대로.
2. 소리 폴링→이벤트 재설계 — EmitSound 즉시 스캔, OnSoundPerceived static 구독, ExpireAfterDelay(5초
   UniTask.Delay + ResponseStarted 체크), TickSoundPerception 폴링 호출 완전 삭제 확인.
3. 방별 인류 인덱스 — _humansByRoom/_humanRoomKey 튜플 키, RegisterUnitPos/UnregisterUnitPos 동일
   지점 갱신, EmitSound가 인덱스만 순회.
4. 15초 워치독 버그 수정 — IsSoundResponse && !SoundPerceptionRolled 예외 확인. 07-A 7-3장 "시작
   기한 5초"는 접근 로직 자체가 아니라 PendingSoundReaction.ValidUntilTime(감지 후 5초 안에
   TryPromotePendingSoundToAlert 안 불리면 폐기)이 담당 — 별도 경로로 문서 규칙을 정확히 충족.
5. 계단 텔레포트 겹침 수정 — TryResolveUnoccupiedStairArrival + CrossStairs/ForceCrossToTargetFloor
   (bool 반환)/RetreatMemberToFloor0(폴백+경고로그) 전부 확인.
6. PropagationDebugVisualizer — OnSoundEmitted 구독, 7개 토글 여전히 존재(아직 임시 코드 상태 유지).
7. FSM 우선순위 — Combat priority 100 vs Tactical 50, 전투 중 IsSoundUnresponsive 차단, TrapResponse
   소리 인터럽트, JoinCombatWait 순위, "Combat 진입 시 currentAlertSearch 안 지워짐" 잔존 이슈까지
   전부 서술 그대로.

**07/07-A 전체 재대조 — 기존 07-31본 장별 태그와도 일치, 새로 발견된 것은 아래 2건뿐(사소함):**
- 6~13장(일반 전파)이 정리_20260805.txt엔 언급이 없었지만(그 문서는 소리 얘기만 다룸) 실제로는
  CorePartySystem/PartyDeathSystem/TrapPartySystem/TacticalFSMState 여러 지점에 이미 광범위하게
  배선돼 있음 재확인 — 07-31본의 ✅ 6장 태그가 최신 코드에서도 여전히 유효.
- 16-5장(전투·도주·퇴각 중 소리)은 여전히 미구현 — 07-31본이 이미 "다음 우선순위 5번, 06문서 부재로
  스텁"으로 기록해 둔 것과 동일, 새 버그 아님.

**새로 찾은 사소한 갭 2건(둘 다 심각도 낮음, 별도 수정 없이 기록만):**
- 07-A 7-2장 동순위 소리 타이브레이크 3단계 중 마지막(랭크 동일·거리도 동일하면 현재 시야 방향과
  가까운 쪽 우선)이 PropagationMath.ShouldReplaceSound에 없음 — 랭크→거리 2단계까지만 구현. 두 소리가
  정확히 같은 거리로 동시 발생하는 극단적 동률 케이스에만 영향(현재는 나중 소리가 이김 — 순서
  의존적).
- PropagationComponent.PendingAttackDir(17장 공격 방향 필드)가 UnitFunction에서 기록만 되고 어디서도
  안 읽힘(전체 grep 확인) — 실제 방향 확인 접근은 AlertSearchState.TargetPosition 경로로 대체돼
  있어 기능 문제는 없으나 죽은 필드. 정리 후보.

결론: 어제 하루 작업 보고서(구현현황_정리_20260805.txt)는 실제 코드 상태를 정확히 반영하고 있고,
문서(07/07-A) 대비 새로운 미구현 사항도 없다 — 위 2건은 기존에 알려진 스텁이 아니라 "이번에 처음
찾은 사소한 단순화/죽은 코드"라 다음 우선순위 목록에 추가한다(아래).

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 갭 해결 (위 재검증에서 찾은 사소한 갭 2건 모두 수정)
════════════════════════════════════════════════════════════════════════
사용자가 "갭 다 해결해봐"라고 요청 — 두 건 다 실제로 고쳤다(다음 우선순위 10/11번 항목 완료로 종료).

1. **07-A 7-2장 시야 방향 타이브레이크 추가.**
   PropagationMath.ShouldReplaceSound에 candidateDirStepDist/currentDirStepDist 선택 매개변수(기본값
   0, 기존 호출부·테스트 호환) 추가 — 랭크 동일 → 거리 동일까지도 같으면 VisionMath.DirStepDistance로
   계산한 "현재 시야 방향(human.currentDir)과의 회전 단계 차이"가 더 작은 쪽을 우선하도록 3단계
   비교로 확장. PropagationSystem.HandleSoundPerceived 호출부에서 SkillAction.GetDirection8(사건
   위치 - 관찰자 위치)로 두 후보의 방향을 구해 넘긴다 — 01-A 11장 VisionMath.ResolveVisionDirection이
   이미 쓰던 것과 동일한 "회전 단계 차이" 개념을 그대로 재사용(새 알고리즘을 만들지 않음).
   테스트: PropagationSystemTests.cs에 ShouldReplaceSound_SameRankAndDistance_
   PrefersCloserToCurrentViewDirection 신규(방향까지 동률이면 기존 소리 유지 확인 포함).
2. **PropagationComponent.PendingAttackDir 죽은 필드 삭제.**
   기록만 되고 어디서도 안 읽히는 걸 재확인(전체 grep으로 재검증) — PropagatedInfoRecord.cs의
   PendingAttackDirection 클래스, PropagationComponent.cs의 필드, UnitFunction.RecordHitWeightEvent의
   기록 블록을 전부 제거했다. 실제 방향 확인 접근 동작(AlertSearchState.TargetPosition 경로)은 이
   필드에 의존하지 않으므로 동작 변화 없음 — CLAUDE.md/시스템 지침의 "확실히 안 쓰는 코드는 완전히
   삭제" 원칙을 따름(부분 스텁으로 남겨두지 않음). 07-A 7-2장이 요구하는 "공격 방향을 다른 유닛에게도
   전파" 자체(발신자→다른 인류로의 실제 브로드캐스트)는 여전히 미구현인데, 이건 이 필드의 사인방향
   재활용으로 때울 수 있는 사소한 갭이 아니라 CanPropagate 기반 전파 경로 신설이 필요한 별도 기능이라
   범위 밖으로 남겨둔다(다음 우선순위 12번 참고).

검증: dotnet build Assembly-CSharp.csproj 성공(오류 0, 경고 39개 그대로 — 신규 경고 없음).

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 새 이슈 해결 (다음 우선순위 12번 — 07문서 7-2장 공격받은 사실의 실제 전파)
════════════════════════════════════════════════════════════════════════
사용자가 "새 이슈 해결해봐"로 요청 — 위 갭 해결 과정에서 새로 찾은 12번(7-2장 브로드캐스트 미구현)도
마저 구현했다.

**구현:** PropagationSystem.PropagateAttackedFact(victim, attackerPosition) 신설, UnitFunction.
RecordHitWeightEvent의 "공격자 미인지" 분기(else, PendingAttackDir를 지웠던 바로 그 자리)에서
victim이 Human일 때 호출한다.
- 7-2장 "이 예외는 발신자의 상태 조건만 예외 처리, 수신자의 상태·동일 공간·전파 범위는 일반 전파
  규칙을 따른다"를 그대로 반영 — CanPropagate(양쪽 비전투 확인) 대신 InPropagationRange(공간+범위만,
  발신자 비전투 조건 없음)를 쓰고, 수신자 쪽 비전투·비상호작용 조건만 IsSoundUnresponsive로 확인한다.
  7-1장 StartJoinCombatWait가 이미 "발견자는 이 사건 자체 때문에 비전투 조건을 못 만족할 수 있다"는
  동일한 이유로 같은 패턴(InPropagationRange 직접 사용)을 쓰고 있어 그 전례를 그대로 따랐다.
- 전달 내용은 문서 그대로 "공격받았다는 사실" + "확인 가능하면 공격 방향"(attackerPosition, 없으면
  null) 둘뿐이다. "공격자를 이미 정확 인지했다면 그 정보도 포함" 조건은 이 메서드가 호출되는 분기
  자체가 "공격자 미인지"일 때만이라 애초에 해당 사항이 없다(별도 처리 불필요).
- 수신자 반응은 17장 "발신자든 수신자든 동일 규칙"에 따라 피해자 본인과 완전히 같은 모양의 일반
  AlertSearchState(03문서 4-10~4-12장 미식별 공격 수색, 기존 15초 워치독 그대로 적용)를 재사용한다 —
  IsSoundResponse 등 소리 전용 필드는 세팅하지 않는다(07문서 2-1장 "전파는 소리가 아니다"). 이미
  currentAlertSearch가 있는(더 급한 상태 처리 중인) 수신자는 덮어쓰지 않는다.

**검증:** dotnet build 성공(오류 0, 경고 39개 그대로). 단위 테스트는 추가하지 않음 —
InPropagationRange가 sender.Session.cmap(방/통로 판정)을 요구해 CanPropagate/StartJoinCombatWait와
동일하게 GameSession/CreateMap 풀 셋업이 필요한 범위라, 기존에도 이 두 함수가 단위 테스트 없이 "실제
플레이 검증" 항목으로 남아있던 것과 같은 사유(전례 일치, 새로 예외를 만들지 않음). 아래 "실제 플레이
검증" 목록에 추가.

07문서 7장 상태 갱신: 07-31본이 7-2를 "AlertSearchState 경로에 통합"으로 ✅ 처리해 둔 것은 사실
"피해자 자신의 반응"만 구현된 상태였다(2026-08-06 재검증에서 발견) — 이번 구현으로 "다른 파티원에게
실제 전파"까지 채워져 이제 명실상부하게 ✅다. 다음 우선순위 12번 항목은 완료로 종료한다.

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 추가 조사 (사용자 "더 찾아봐") — 07문서 10장 중복 조사 버그 발견 + 수정
════════════════════════════════════════════════════════════════════════
fork로 아직 안 본 영역(9장 합류판정 세부/8장 사망정보/코어·함정 전파조건/공간거리BFS/16-6몬스터/10장
보호포메이션)을 추가로 훑었다. 대부분 CONFIRMED(문제없음)였고, 실제 버그 1건을 새로 찾아 바로 고쳤다.

**CONFIRMED(문제없음, 상세 생략) — 9장 합류판정(2초/5초/선공시 즉시전투 전부 정확), 8장 사망정보
(기록/정신력/전파 3종 다 있음, TickOngoingPropagation은 UnitFunction.OnUpdate에서 실제로 매 틱
호출되는 살아있는 코드), 코어·함정 전파의 "발신자 보유 정보만" 게이팅, TryGetSpaceDistance BFS(문서
예시 "직선2·우회4→범위3 실패/범위4 성공" 그대로 재현, 코너 커팅 방지 포함), 16-6장 몬스터 소리(스텁
그대로, 죽은 코드 조각 없음).**
"합류 대기 중 발견자의 방어 계산 활성화"(7-1장)는 TacticalFSMState.cs 주석에 이미 "06_전투반응·기습
문서 부재로 스텁"이라 명시돼 있던 기존 스텁 — 새 발견 아님.

**새로 찾은 버그: 07문서 10장 마지막 문단 미구현 — 일반 조사 중복 선택.**
"재전파 수신자는 보호 포메이션에는 참여하지 않지만, 같은 대상에 대한 중복 조사·해제 목표를 선택하지
않는다"에서 뒷부분("중복 조사·해제 목표 선택 금지")이 함정(TrapPartySystem.TrapPartyCoordination,
SelectionLocked)에만 구현돼 있고 일반 조사(Human.ComputeInvestigateTarget)에는 전혀 없었다 — 파티원
둘이 동시에 같은 오브젝트를 향해 조사를 시작할 수 있는 실제 버그.
**수정:** Unit.cs의 Human.ComputeInvestigateTarget(조사 후보 스캔 루프)에 IsInvestigationClaimedByPartyMember
게이팅 추가 — 다른 파티원이 이미 그 오브젝트를 currentInvestigation.TargetObjectId로 갖고 있으면 후보에서
제외한다. TrapPartySystem처럼 별도 코디네이션 dict를 새로 만들지 않고, 파티 순회로 즉석 확인하는 더
가벼운 방식을 택했다(파티 규모가 작아 매 프레임 캐시 재계산 비용에 무리 없음 — FindInvestigateTarget이
이미 프레임 단위로 캐시하고 있어 추가 비용도 프레임당 1회로 제한됨).
검증: dotnet build 성공(오류 0, 경고 39개 그대로). 단위 테스트는 Session/objectGrid/party 풀 셋업이
필요해 기존 ComputeInvestigateTarget과 동일 사유로 추가 안 함 — 실제 플레이 검증 항목에 추가(파티원
두 명이 같은 미발견 오브젝트를 동시에 알게 됐을 때 한 명만 조사를 시작하는지).

**07 범위 밖 참고 — 2026-08-06 "1순위들 다해봐"에서 마저 삭제 완료.** PartyDeathRecord.
CauseConfirmedDirectly(03문서 소속) 죽은 필드는 아래 "1순위 재정리" 3번에서 처리.

════════════════════════════════════════════════════════════════════════
■ 다음 우선순위 재정리 (2026-08-06, 오늘 완료분 정리 후 남은 것만 — 위 1~12번 중 완료 표시 안 된
  것 + 03문서 쪽에서 오늘 작업과 맞물리는 항목 포함)
════════════════════════════════════════════════════════════════════════
오늘 하루로 07/07-A 문서는 사실상 전 장(1~18장) 검증 완료 + 발견된 실제 버그 4건(15초 워치독 오적용,
계단 겹침, 7-2 브로드캐스트 누락, 10장 중복 조사) 전부 수정 상태다. 남은 건 "문서 부재로 못 만드는
큰 것들"과 "당장 만들 수 있는 작은 정리"로 갈린다.

**0순위 — 코드 작업 아님, 선행 필수:** 오늘 고친 것 전부(E_HIT_HEAVY_INDIRECT/E_MONSTER_KILL_INDIRECT,
소리 이벤트 재설계, 방별 인덱스, 15초 워치독, 계단 겹침, 타이브레이크, 7-2 브로드캐스트, 중복 조사
방지) 실제 플레이 검증이 전무하다 — 전부 dotnet build + 손계산 테스트로만 확인됐다. 다음 세션은 새
기능을 더 만들기보다 먼저 실제로 켜서 확인하는 걸 최우선으로 권장한다(위 "실제 플레이 검증" 목록
1~10번이 체크리스트).

**1순위 — 2026-08-06에 전부 처리(사용자 "1순위들 다해봐"):**
1. [완료] **03문서 4-5장 AlertSearchState 정리 누락 버그.** CombatFSMState.OnEnter(빈 메서드였음)에
   currentAlertSearch=null 추가 — 문서 원문은 "OnExit"을 지목했지만, 전투 중엔 IsSoundUnresponsive가
   새 소리 반응 생성 자체를 막아 OnExit 시점의 currentAlertSearch는 항상 "전투 진입 전 잔재 또는
   null"뿐이라 OnEnter에서 미리 비우는 쪽이 더 정확하다(OnExit의 기존 "null이면 전투 후 스윕 세팅"
   로직도 그대로 살아있음). TrapDisarmPerform(해제 성공)/TrapBypass/TrapPass(문서엔 없었지만 동일
   패턴이라 추가)/TrapDestroy(전부 성공 반환 직전) + 실제 조사 종료 지점인 PickUpObject(두 반환
   지점)/CoreInvestigatePerform(두 반환 지점, 동일 BT 구조라 함께 처리)에 currentAlertSearch=null
   명시적 연결 완료. 03문서 쪽 구현현황(시야인지반응_03_구현현황_2026-07-27.txt "다음 우선순위 1번")도
   완료로 갱신.
2. [완료] **10장 NotifiedActiveInteractions 인스턴스 구분.** PropagationComponent.
   NotifiedActiveInteractions(HashSet<Unit>)를 NotifiedInteractionTokens(Dictionary<Unit, object>)로
   교체 — PropagationSystem.GetInteractionToken(unit)이 currentTrapInteraction/currentInvestigation/
   currentCoreInteraction 중 non-null인 것의 참조 자체를 "이번 상호작용 인스턴스"의 토큰으로 쓴다(이
   상태 객체들은 TacticalFSMState가 상호작용을 새로 시작할 때마다 `new`로 교체하므로 별도 ID 체계 없이
   인스턴스 구분이 됨). NotifyInteractionStarted가 토큰을 같이 저장하고, HasReceivedInteractionNotice는
   저장된 토큰이 상호작용 유닛의 "지금 이 순간" 토큰과 참조가 같을 때만 true — 상호작용이 끝나거나
   완전히 새 상호작용으로 바뀌면 자동으로 무효화된다.
3. [완료, 07 범위 밖] **PartyDeathRecord.CauseConfirmedDirectly 죽은 필드 삭제.** PendingAttackDir와
   동일 패턴 재확인(4개 쓰기 지점, 읽는 곳 0곳) — ApplyDangerOnce가 이미 호출부에서 명시적 EventId/
   InfoType(E_HUMAN_KILL_SEEN/DirectWitness vs E_HUMAN_KILL_INDIRECT/Indirect)를 직접 받고 있어 이
   필드가 애초에 중복 정보였다. PartyDeathRecord.cs 필드 + PartyDeathSystem.cs 4개 쓰기 지점 전부 제거.
4. [결정] **PropagationDebugVisualizer 존치.** 0순위(실제 플레이 검증)가 아직 하나도 안 끝나 지금
   지우면 검증 도구 자체가 없어진다 — "삭제할지 사용자 판단 필요"였던 항목을 "0순위 검증 끝날 때까지
   보류"로 직접 결정하고 그대로 뒀다(코드 변경 없음, 되돌리기 쉬운 선택).
5. [재확인, 코드 변경 없음] **몬스터 무리형 전파/소리 감지.** 실제로 손대볼지 검토했으나, 07문서
   16-6장이 "몬스터는 소리 감지+방향·추정지역 획득까지만 07 담당, 반응 해석은 각 역할군 AI 몫"이라고
   명시하는데 그 역할군 AI 시스템 자체가 없다 — 지금 소리 감지를 몬스터까지 연결해도 아무도 그 결과를
   못 읽는 죽은 데이터만 새로 만드는 셈이라(PendingAttackDir/CauseConfirmedDirectly와 같은 패턴을
   스스로 만드는 꼴) 손대지 않았다. 역할군 AI 문서/시스템이 생길 때 같이 연결하는 게 맞다.

빌드: 위 1~3번 전부 dotnet build 확인 완료(오류 0, 경고 39개 그대로, 매 항목마다 개별 확인).
단위 테스트는 추가 안 함 — 1번은 FSM 리프 완료 지점 단순 대입이라 기존 FSMBehaviorTests 스타일로
Session 셋업 없이 값만 검증하긴 애매하고(실제로는 human.OnUpdate 등 전체 틱 흐름이 얽힘), 2번은
Session/party 풀 셋업이 필요해 CanPropagate류와 동일 사유로 실제 플레이 검증 항목에 위임.

**2순위 — 문서 부재로 여전히 보류(문서가 나오면 바로 손댈 것들, 지금은 대기만):**
- 09문서(목표·이동경로): 07-A 11장 합류 5초 초과 처리, 코어 발견 물리적 전파, 08/09장 정식 파티
  통신 체계.
- 06문서(전투반응·기습): 16-5장 전투 중 소리 기반 새 타겟 탐색, 15장 반응 후보 실제 선택 로직,
  합류 대기 중 "방어 계산 활성화"(7-1장).
- 함정 시스템 문서: 함정 피해 인원수·단계별 위험도 증가량, 함정 흔적 이해도 상승량(수치만 정해지면
  소리 이벤트 자체는 이미 있어 바로 연결 가능).

**3순위 — 다른 시스템(참고용, 이 문서 범위 밖):** 가중치_구현현황 문서의 남은 큰 항목(버프/디버프/
소환 시스템, 13-1장 레벨 총합 보정값 — 둘 다 전투/레벨 시스템 쪽 선행 설계 필요). 작은 항목 중
19장 전멸 흔적 흥미도 보정은 2026-08-06에 완료(WeightMath.WipeoutTraceInterest 신설 +
PersonalMapKnowledge.RegisterObject 수정 + 테스트 추가 — 가중치_구현현황_2026-07-20.txt "다음
우선순위" 0번 참고). 03_구현현황 문서의 나머지 작은 항목은 9-6장 인접 1칸 "지속" 회피(지금은
발견 순간 1회성 회피뿐)와 6-4장 "완전히 막히면 우회" 방어책의 MoveToInvestigateTarget/MoveToTrap
확장 — 둘 다 문서 부재로 막힌 게 아니라 지금 바로 손댈 수 있는 작은 항목이다(2026-08-06 재확인,
같은 목록에 있던 "4-13장 진짜 연속 재전파"는 이미 07문서 9장 작업으로 해결된 낡은 항목이라 제외 —
03문서 쪽에도 반영해 둠). 필요하면 각 문서의 "다음 우선순위" 섹션 참고.

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 채팅 검증 중 발견 + 수정: 07문서 1장 "소리 감지: 인류/몬스터 모두 적용" 실제로는 인류만
════════════════════════════════════════════════════════════════════════
사용자가 07문서를 1장부터 채팅으로 실제 코드와 대조 검증하는 중 — 1장 표("소리 감지: 인류·몬스터
모두 적용")를 확인해보니 EmitSound의 스캔 대상 인덱스(_humansByRoom)가 Human만 등록해 몬스터는
애초에 스캔 후보에 들 수 없었다. 사용자가 "몬스터 소리쪽은 다 살려줘. 전파는 일단 없어도 됨"으로
확정 — 전파(6~11장)는 그대로 인류 전용 유지, 소리 감지·반응(14~17장 중 소리 부분)만 몬스터까지 확장.

**바로 위 "1순위 재정리" 5번의 판단이 틀렸다는 걸 재확인 중 발견했다.** 5번은 "16-6장이 반응 해석을
역할군 AI 몫으로 넘기는데 그 AI가 없어서, 소리 감지를 몬스터까지 연결해도 아무도 못 읽는 죽은 데이터"
라고 판단해 보류했었다 — 그런데 실제로는 TacticalFSMState의 Alert 브랜치(HasAlert→SoundMoveReact/
SoundAreaApproach/AlertApproach, currentAlertSearch)가 처음부터 Unit 기준으로 일반화돼 있고,
Monster.JudgeState()도 base.JudgeState()를 그대로 호출해 UnitFSM(Combat/Tactical/Navigation 전부
Unit 매개변수)을 몬스터에도 동일하게 돌린다 — 즉 "읽는 쪽"이 이미 존재했고, Human으로 고정된 두 지점
(TacticalFSMState.HasAlert의 승격 호출, PropagationSystem의 방 인덱스·스캔)만 막혀 있었다. 5번 항목의
전제(역할군 AI 부재 = 죽은 데이터)는 틀렸던 것으로 정정한다.

**수정 지점(로직 변경 없이 대부분 Human→Unit 타입 일반화, 실제 새 로직은 두 곳뿐):**
- `PropagationSystem.cs`: `_humansByRoom`/`_humanRoomKey` → `_listenersByRoom`/`_listenerRoomKey`
  (Dictionary 값 타입 Human→Unit), `UpdateHumanRoomIndex`→`UpdateListenerRoomIndex`,
  `RemoveFromRoomIndex`/`SoundPerceivedEvent.Observer`/`HandleSoundPerceived`/`ExpireAfterDelay`/
  `TryPromotePendingSoundToAlert` 전부 Human→Unit. **새 로직**: (1) `EmitSound` 스캔 루프의 16-2장
  이동음 제외 조건을 "source is Human"(인류 이동음 전체 제외)에서 "발신자·감지자가 같은 종족일 때만
  제외"(sourceIsHuman == listenerIsHuman)로 바꿔 몬스터가 인류 이동음은 듣고 몬스터끼리 이동음만
  서로 무시하게 함 — 반대로 인류도 몬스터 이동음은 그대로 듣는다(기존 동작 유지). (2)
  `SoundDetectionRange` 호출의 `isMonster` 인자를 `false` 고정에서 `!listenerIsHuman`으로 바꿔
  `MonsterHearingBonusTiles`(+2칸)가 실제로 적용되게 함 — 이 상수는 5-2장 도입 때부터 있었지만
  호출부가 없어 죽은 매개변수였다.
- `IsSoundUnresponsive`: Human→Unit, 파티목표·코어상호작용·합류대기 게이팅은 `unit is Human`
  분기로 좁혀 인류 전용 유지(몬스터는 해당 필드 자체가 없음), "이미 전투 목표 있으면 무시"
  (personalSpottedEnemies)는 공통 규칙이라 그대로 모든 유닛에 적용.
- `GameSession.RegisterUnitPos`/`UnregisterUnitPos`: `if (u is Human)` 게이팅 제거, 모든 Unit을
  `UpdateListenerRoomIndex`/`RemoveFromRoomIndex`에 등록.
- `TacticalFSMState.HasAlert`: `(unit is Human human && TryPromotePendingSoundToAlert(human))` →
  `TryPromotePendingSoundToAlert(unit)` (Human 게이팅 제거 — 이게 실제로 몬스터를 막고 있던 핵심
  지점). `ClearSoundAlert`: `if (unit is Human human) human.Propagation.PendingSound = null`도
  Human 게이팅을 제거하지 않으면 몬스터의 PendingSound가 영영 안 지워져 SoundMoveReact 완료 후에도
  새 소리를 못 받는 상태로 고착되는 버그가 생겨 함께 고쳤다.
- `TryConfirmIndirectHit`/`OnMonsterCorpseDiscovered`/전파 관련 함수(`CanPropagate`/
  `StartJoinCombatWait`/`PropagateAttackedFact`/`NotifyInteractionStarted` 등)는 그대로 Human 전용
  유지 — E_HIT_HEAVY_INDIRECT는 HumanKnowledgeBase(인류 전용 가중치 시스템) 연결이라 몬스터에 해당
  사항 없고, 전파는 사용자가 명시적으로 "일단 없어도 됨"이라 손대지 않았다.

**남은 갭(16-6장 관점):** 몬스터는 이제 인류와 동일한 SoundMoveReact/SoundAreaApproach/AlertApproach
(시야 전환+2초 유지, 또는 추정 지역 접근+인지판정 1회)를 그대로 재사용한다 — 16-6장이 말하는 "각
역할군 AI가 감지 결과를 해석"하는 몬스터 전용 반응(예: 역할군별로 다른 접근 방식·공격 우선순위)은
여전히 없고, 이 재사용이 그 자리를 메우는 가장 단순한 기본값이다(CLAUDE.md 관례). 역할군 AI 시스템이
생기면 그때 몬스터 전용 반응으로 교체.

**테스트:** `PropagationSystemTests.cs`에 `OnSoundPerceived_MonsterListener_SetsPendingSound`(몬스터도
Observer가 될 수 있고 PendingSound가 채워지는지)와 `TryPromotePendingSoundToAlert_PromotesForMonsterToo`
(승격이 Human 게이팅 없이 몬스터에도 동작하는지, 이번 수정의 핵심 지점) 2건 추가.

**검증:** `dotnet build GrimArchive_Prototype.slnx` 성공(오류 0개, 경고는 기존 프로젝트 전반 것과 동일
— 이번 변경 파일에서 새로 발생한 경고 없음 확인). Unity Test Runner를 통한 실제 EditMode 테스트 실행과
실제 플레이 검증(몬스터가 인류 이동/전투 소리에 실제로 반응해 접근하는지)은 이 세션에서 수행하지
않음 — 기존 "실제 플레이 검증" 목록에 추가할 것.

**1순위 재정리 5번 상태 갱신:** "재확인, 코드 변경 없음" → 완료(전제 오류 정정 + 구현). 전파/무리형
몬스터 재전파(16-6장의 "전파 기능이 있는 무리형 몬스터 예외")는 여전히 미구현 — 사용자가 이번엔
범위 밖으로 명시했다.

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 채팅 검증 계속: 07문서 3~5장(정보 보유 구조) — 전파 정보 vs 개인 지도 분리/병합 결정
════════════════════════════════════════════════════════════════════════
사용자가 07문서(개념)를 3장부터 이어서 채팅으로 검증하다가, 3장 표("직접 정보/전파 정보/일시
간접입력")와 4장("전파받은 정보가 구체적이어도 수신자의 직접 인지 결과로 취급 안 함"), 13장("더
최신의 확인 정보가 들어오면 갱신, 오래된 정보는 유지")을 근거로 설계 결정을 요청 — "전파 정보
(PropagatedInfoRecord)와 개인 지도(PersonalMapKnowledge)를 분리 보관할지, 병합할지".

**조사 결과 (결정 전 확인):** PropagatedInfoRecord는 이미 PropagationComponent.PropagatedInfo
(Dictionary<object, PropagatedInfoRecord>)로 PersonalMapKnowledge와 분리 저장 중이었지만, 5장이
나열한 7개 전파 대상 정보 카테고리 중 "적 마지막 확인 위치" 1개만 구현돼 있었고(RecordEnemySighting,
7-1장 합류 판정 전용) 그마저도 write-only — 09(목표·경로) 문서 부재로 아무 데서도 안 읽히고 있었다.
반면 PersonalMapKnowledge.ObserveMonster는 이미 WeightMath.PriorityRank/ShouldReplace(가중치
문서 24장, 직접>간접 항상 우선)로 InfoType 태그 기반 우선순위 병합을 하고 있어, "같은 구조를 재사용해
병합"도 실행 가능한 대안이었다.

**사용자 결정:** 저장소는 분리 유지 + 판단(조회) 시점에는 24장 PriorityRank(직접이 오래됐어도 항상
우선)가 아니라 13장 자체의 순수 최신성 규칙만 적용 — 동일 대상 기준 두 저장소 중 타임스탬프가 더
최신인 쪽 값을 쓴다. 사용자가 "지금 바로 코드에 반영"까지 요청해 그 자리에서 구현했다.

**구현:**
- `PersonalMapKnowledge.cs`의 private `MonsterSighting`에 `Timestamp` 필드 신설(`ObserveMonster`가
  `Time.time`으로 채움) — 기존엔 이 최신성 비교를 할 방법 자체가 없었다(RecordedInfoType만 있고
  시각이 없었음). `TryGetMonsterSighting`에 timestamp를 얹은 오버로드 추가(기존 3-out 시그니처는
  호환용으로 유지, 호출부 없어 실질적으로는 그대로 대체해도 무방했지만 최소 변경으로 처리).
- `PropagationSystem.cs`에 `GetLatestKnownPosition(Human observer, Unit target, out tile, out
  timestamp)` 신설 — `PropagationMath.ShouldUpdateLastKnownPosition`(07-A 3장, 이미 존재하던
  순수 비교 함수 `candidateTimestamp > existingTimestamp`)를 그대로 재사용해 직접/전파 두 기록의
  타임스탬프만 비교한다(새 비교 로직을 만들지 않음 — 기존 07-A 3장 함수가 정확히 이 규칙이었다).
  둘 다 없으면 false, 하나만 있으면 그 값, 둘 다 있으면 최신 쪽.
- `PropagatedInfoRecord.cs` 상단 주석에 이 결정(저장소 분리 vs 판단 병합)을 명시해 근거를 남겼다.
- 이 함수도 아직 호출부가 없다(PropagatedInfo 자체가 write-only인 것과 동일 사유 — 09문서가 실제
  "마지막 확인 위치를 목표/경로에 반영"할 소비처다). `PropagatedInfoRecord`/`RecordEnemySighting`과
  같은 성격의 "미리 준비해 둔 배선"으로 남긴다.

**테스트:** `PropagationSystemTests.cs`에 4개 신규 — `GetLatestKnownPosition_ReturnsFalse_
WhenNeitherRecorded`/`_UsesDirect_WhenOnlyDirectRecorded`/`_PrefersNewerPropagated_OverOlderDirect`
(24장이었다면 오래된 직접이 이겼겠지만 13장 규칙으로는 최신 전파가 이김을 못박음)/`_KeepsDirect_
WhenNewerThanPropagated`. `Time.time`을 테스트에서 직접 통제할 수 없어(EditMode, PlayMode 미실행)
비교 대상 한쪽은 명확히 극단값(999f/-100f)으로 고정해 순서를 확정했다.

**검증:** `dotnet build GrimArchive_Prototype.slnx` 성공(오류 0개, 경고 88개 — 변경 파일 자체에서
새로 발생한 경고 없음, grep으로 확인). Unity Test Runner 실행은 여전히 안 함 — 기존 "실제 플레이
검증"/EditMode 실행 대기 목록에 이번 4개도 합류.

사용자는 이 결정 이후 07문서 검증을 계속 이어갈 예정 — 다음은 6장(일반 전파 조건)부터.

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 채팅 검증 계속: 07문서 6~8장 — 실제 버그 2건 발견 + 즉시 수정
════════════════════════════════════════════════════════════════════════
6장(일반 전파 조건) 자체는 CanPropagate/InPropagationRange가 문서 5개 조건과 07-A 1장 SameSpace/
TryGetSpaceDistance(우회 BFS, 코너 커팅 방지, 문서 예시 "직선2·우회4→범위3 실패/범위4 성공") 그대로
일치 — 새 문제 없음. 7장(전투 진입 예외)도 7-1(StartJoinCombatWait+RequiresJoinWait)이 07-A 9장
수치표(근거리 2칸/위험도2+합류대기/응답대기2초/합류최대대기5초)와 정확히 일치, 7-2(PropagateAttackedFact)는
기존 확인 그대로, 7-3(전투합류 예외 전파)은 08/09 문서 부재로 여전히 스텁(기존에 이미 기록된 항목,
새 발견 아님) — CONFIRMED. 다만 6장이 참조하는 07-A 1장("전파·소리 **공통** 공간 거리 판정")과 8장
(사망 정보 특례)을 실제로 더 깊이 대조하다가 지금까지의 fork 감사가 못 잡은 실제 버그 2건을 새로
찾아 사용자 승인 즉시 고쳤다.

**버그 1: 소리 감지가 "공통" 공간 거리 판정을 실제로 안 쓰고 있었다.**
07-A 1장 제목이 "전파·소리 공통 공간 거리 판정"인데, 전파(InPropagationRange)는 벽 우회 BFS
(PropagationMath.TryGetSpaceDistance)를 쓰는 반면 소리(EmitSound)는 순수 직선거리(Vector2Int.
Distance)만 썼다 — 방 안 장애물(isStructureExist)이 있으면 문서가 요구하는 "우회 최단경로"와
결과가 달라질 수 있다. 기존 08-05/08-06 fork 감사가 "TryGetSpaceDistance BFS(문서 예시 그대로 재현)"
로 CONFIRMED 처리한 건 이 순수 함수 자체의 정확성만 검증한 것이었고, EmitSound가 아예 이 함수를
안 쓰고 있다는 사실은 놓치고 있었다.
**수정:** `EmitSound`의 `Vector2Int.Distance` 최종 판정을 `PropagationMath.TryGetSpaceDistance(position,
listener.position, detectRange, p => cmap.IsStaticTileWalkable(floorIndex, p) && cmap.GetRoomIdAt(floorIndex, p) == roomId, out _)`
로 교체. `_listenersByRoom` 인덱스로 이미 같은 방 후보만 남지만, 우회 경로 자체가 게이트를 스쳐
옆방/통로로 새지 않도록 각 후보 타일도 `roomId` 일치를 추가로 확인한다.

**버그 2: 전파의 벽 우회 BFS가 "같은 공간 안에서만" 우회하지 않았다.**
`InPropagationRange`가 쓰는 walkable 판정(`cmap.IsStaticTileWalkable`)은 구조적 통행 가능 여부만
보고 방 소속은 안 봐서, 우회 경로가 게이트를 지나 옆방/통로를 잠깐 스치는 경우까지 유효 경로로
잡을 수 있었다 — 07-A 1-2장은 "같은 공간 안에서 이동 가능한 최단 경로"라고 명시.
**수정:** predicate에 `&& cmap.GetRoomIdAt(floor, p) == senderRoom` 추가(이미 SameSpace로 확인된
senderRoom 재사용, 새 조회 없음).
두 버그 모두 이 맵 생성기가 대체로 내부 장애물 없는 사각형 방을 만들어 실전 영향은 작을 가능성이
높지만(사용자도 이 점 인지 후 "둘 다 지금 고치기" 선택), 방 안에 `isStructureExist` 구조물이 있는
경우엔 실제로 결과가 달라진다.

**버그 3: 사망 정보 전파가 "비전투 조건"에 막히고 있었다(07문서 8장/03문서 4-12장 위반).**
07문서 8장: "파티원 사망 정보는 일반적인 비전투 전파 조건보다 사망 정보 규칙을 **우선**한다."
03문서 4-12장: "사망 정보 기록, 정신력 감소와 최초 전파는 유닛의 **현재 상태**와 탐색 반응
우선순위에 **관계없이** 즉시 처리한다." — 이미 7-1(StartJoinCombatWait)/7-2(PropagateAttackedFact)가
동일한 이유로 CanPropagate(비전투 조건 포함) 대신 InPropagationRange(공간+범위만)를 쓰고 있는데,
`PartyDeathSystem.PropagateFrom`(최초 전파)/`TickOngoingPropagation`(재전파)은 여전히 CanPropagate를
쓰고 있어 대표 발견자나 수신자 중 한쪽이라도 전투 중(personalSpottedEnemies.Count>0)이면 사망
정보 전파 자체가 막히는 실제 버그였다.
**수정:** `PropagationSystem.InPropagationRange`를 private→public 승격(7-1/7-2와 같은 파일 안에서만
쓰이던 걸 다른 파일에서도 재사용). `PartyDeathSystem.PropagateFrom`/`TickOngoingPropagation`의
`CanPropagate` 호출 2곳을 `InPropagationRange`로 교체 — 사망 정보 자체(기록·정신력·전파)는 전투
중에도 공간+범위 조건만으로 즉시 오간다. 막히는 건 여전히 "경계 수색으로 전환"뿐(그건 원래도
현재 행동 우선순위를 따르는 게 맞음 — CanJoinDeathSearch가 담당, 안 건드림).

**검증:** 세 수정 모두 `dotnet build GrimArchive_Prototype.slnx` 성공(오류 0개, 경고 88개 그대로,
변경 파일에서 새 경고 없음 확인). 단위 테스트는 추가하지 않음 — 셋 다 EmitSound/InPropagationRange/
PartyDeathSystem 전부 기존에도 GameSession/CreateMap 풀 셋업이 필요해 단위 테스트 없이 "실제 플레이
검증" 항목으로 남아있던 함수들이라(전례 일치, 새 예외 안 만듦) 그대로 따랐다. 실제 플레이 검증
목록에 추가:
  11. 방 안에 장애물(isStructureExist)이 있을 때 소리/전파가 직선이 아니라 실제 우회 경로 기준으로
      판정되는지, 우회가 옆방/통로를 스치는 경로는 실패하는지.
  12. 파티원이 전투 중일 때(자신이 다른 몬스터와 교전 중) 다른 파티원의 사망 정보를 즉시 받는지
      (예전엔 막혔던 케이스), 반대로 전투 중인 목격자가 대표 발견자가 됐을 때도 즉시 전파하는지.

사용자는 이후 07문서 나머지 장(9장 이후) 검증을 계속 이어갈 예정.

════════════════════════════════════════════════════════════════════════
■ 2026-08-06 채팅 검증 계속: 07문서 9장 — 함정 정보 지속 재전파 누락 발견 + 수정
════════════════════════════════════════════════════════════════════════
9장 "재전파와 정보 동기화": "이후 전파 판정에서 일반 전파 조건을 만족하는 미보유 유닛이 존재하면
해당 정보를 재전파할 수 있다." 사망 정보(PartyDeathSystem.TickOngoingPropagation)와 코어 정보
(CorePartySystem.TickLeaderPropagation)는 이미 매 틱 재확인하지만, **함정 정보(TrapPartySystem)는
OnTrapDiscovered 시점 1회 전파뿐이고 이후 재확인이 없었다** — 발견 당시 전파 범위 밖에 있던
파티원은 그 함정을 영원히 모른 채로 남는 실제 버그. 9-6장 회피 로직과 위험도 인지 모두
personalMap.IsObjectKnown에 의존하는데 이 정보 자체가 안 들어오니 실제 영향도 있었다.

**수정:** `TrapPartySystem.TickOngoingPropagation(Human human)` 신설 — party.TrapCoordinations를
순회하며 아직 모르는 함정마다, 이미 아는 파티원(carrier) 중 CanPropagate(비전투+같은 공간+전파범위,
9장은 8장 사망정보와 달리 예외 규칙이 없어 일반 CanPropagate 그대로 사용 — OnTrapDiscovered/
ResolveSelection과 동일 기준) 조건을 만족하는 사람이 있으면 objectGrid에서 InteractableObject를
다시 조회해 RegisterObject로 등록한다(PartyDeathSystem/CorePartySystem과 동일 구조). UnitFunction.
OnUpdate의 기존 사망/코어 재전파 틱 블록(0.1초 간격) 바로 옆에 호출 추가 — 새 폴링 루프를 만들지
않고 이미 있는 자리에 얹었다.

**검증:** `dotnet build` 성공(오류 0개, 새 경고 없음). 단위 테스트는 추가하지 않음 — Session/
objectGrid/party 풀 셋업이 필요해 PartyDeathSystem/CorePartySystem과 동일 사유로 기존에도 단위
테스트 없이 "실제 플레이 검증" 항목으로 남아있던 영역. 실제 플레이 검증 목록에 추가:
  13. 발견 당시 범위 밖에 있던 파티원이 나중에 함정 근처로 접근하면 실제로 함정 정보를 받는지
      (personalMap.IsObjectKnown이 true로 바뀌는지), 9-6장 인접 회피가 뒤늦게라도 걸리는지.

사용자는 이후 07문서 나머지 장(10장 이후) 검증을 계속 이어갈 예정.

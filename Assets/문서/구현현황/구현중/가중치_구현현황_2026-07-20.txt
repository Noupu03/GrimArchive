GrimArchive 대표 가중치 3종(이해도/위험도/흥미도) 구현 현황 — 2026-07-20 재검증본
기준 문서: GrimArchive_대표가중치_연산공식_v0.7_양식정리.docx (1).md
         (Assets/문서/공식문서/가중치 관련/ — "(1)" 개정판이 최신본)
작성일: 2026-07-20 (이전 버전은 구현현황/지난거/가중치_구현현황_2026-07-09.txt)
동반 문서: 같은 폴더의 `가중치_수정예정_2026-07-20.txt` — 이번 재검증에서 찾은 보류 항목 1개.

이 버전은 새 구현 라운드가 아니라 **전면 재검증**이다 — 07-09 이후 11일 동안 이 시스템의 코드
(WeightMath.cs/HumanKnowledgeBase.cs/PersonalWeightRecord.cs/IncidentLog.cs/SpeciesWeightState.cs/
weight_events.json/WeightEventTable.cs/WeightEnums.cs)를 문서 1장부터 25장까지 전부 다시 읽고
대조했다. 결론: **07-09 문서 내용은 전부 여전히 정확하다** — 이 기간 동안 이 시스템 자체에는 실질
변경이 없었다(같은 기간 진행된 건 시야-인지 시스템 쪽, 구현현황/시야인지반응_구현현황_2026-07-20.txt
참고). 아래 본문은 07-09 버전을 그대로 유지하되, 이번 재검증에서 새로 확인/발견한 것만 상단에
추가하고 해당 장 항목에 표시를 남겼다.

태그
  ✅ 구현       — 공식/수치가 코드에 정확히 있고, 조회 가능(테스트로 고정된 것 다수).
  🔶 부분/수치만 — 공식은 있지만 실제 게임 이벤트가 자동으로 그 지점을 타지는 않음(수동 호출 API).
  ❌ 미구현     — 코드에 없음.
  🆕 (1) 개정   — 원본에 없다가 이번 (1) 개정판에서 새로 생긴 내용.
  🔵 07-20 재검증 — 이번 재검증에서 새로 확인/발견한 내용(값 자체가 바뀐 건 아니고 이해/기록이 갱신됨).

════════════════════════════════════════════════════════════════════════
■ 2026-07-20 재검증에서 새로 확인/발견한 것 2가지
════════════════════════════════════════════════════════════════════════
1. [3-1장/11-2장 — 새로운 교차 문서 의존성 발견, 버그 아님] 같은 날 진행된 02_인지·정보판정·
   실패처리_시스템 문서 17장("공격자 정체는 별도 인지 판정 필요") 구현으로 UnitFunction.
   RecordHitWeightEvent에 공격자 정체 게이팅을 추가했는데, 이게 이 문서의 E_HIT_HEAVY_SELF/SEEN과
   11-2장(ApplyPerHitDangerDecreaseCheck)에도 실제로 영향을 준다 — 이제 둘 다 "피격 대상이 공격자의
   정체를 인지 판정으로 확인해야만" 발동한다(예전엔 피해량 조건만 보고 무조건 발동했음). 07-09
   시점엔 이 의존성이 존재하지 않았다(그 게이팅 자체가 없었음).
   타당성 검토: 11-2장 계산 자체가 "몬스터의 타격 1회별 기준 피해량 - 실제 적용 피해량"인데, 애초에
   어떤 몬스터(종)에게 맞았는지 모르면 비교 기준(그 종의 기준 피해량)을 알 수 없다 — 정체 게이팅이
   오히려 계산을 논리적으로 성립시키는 전제 조건이라 타당한 의존성으로 판단했다. 코드는 이미
   반영되어 있다(UnitFunction.cs, 오늘 다른 세션에서 완료). 아래 3-1장/11-2장 항목에 🔵 표시.
2. [19장 — 사용자 확인, 코드는 아직 미반영] 전멸 흔적(WipeoutTrace) 흥미도가 시체/흔적과 달리
   원인 대상 위험도 단계 보정을 안 받고 고정 25만 쓰는 걸 발견 — 13-2장(전멸 흔적 발견 전역
   반영값)은 이미 동일 구조에 보정을 적용하고 있어서 비대칭이었다. "보정 적용" 쪽으로 확정했고,
   제안 코드까지 `가중치_수정예정_2026-07-20.txt` 항목 1에 작성 완료 — 아직 코드 반영 전(다른
   작업자와 PersonalMapKnowledge.cs가 겹칠 수 있어 보류). 아래 19장 항목에 🔵 표시.

════════════════════════════════════════════════════════════════════════
■ 다음 우선순위 구현 후보 (2026-07-09 정리, 위에서 아래로 갈수록 작업량이 커짐 — 2026-07-20
  재검증 결과 전부 그대로 유효, 상태 변화 없음)
════════════════════════════════════════════════════════════════════════
아래는 "이번 주 안에 오브젝트→방→파티→시체/흔적 순으로 블로커가 풀려온 흐름"의 연장선에서,
지금 남은 미구현/부분구현 중 실제로 다음에 손댈 만한 것들을 작업량 순으로 정리한 것이다. 남은
항목 대부분이 "이 이벤트를 발생시킬 하위 게임 시스템이 아직 없다"는 동일한 원인으로 수렴한다.

0. [신규, 코드 반영 전] 가중치_수정예정_2026-07-20.txt 항목 1(19장 전멸 흔적 흥미도 보정) —
   판단은 끝났고 제안 코드도 있음, 다른 작업자와 파일이 안 겹치는지만 확인하면 바로 적용 가능.

1. [완료, 2026-07-09 점검에서 처리] 13-2장 전멸 흔적 "발견→전역 반영" 연결
   전멸 흔적 오브젝트(InteractableObject, Tags=["WipeoutTrace"])는 이미 스폰되고 있었지만
   RegisterWipeoutTrace()가 발급한 traceId를 들고 있지 않아 CastRay가 발견해도
   OnWipeoutTraceReflected()를 부를 방법이 없었다. InteractableObject.TraceId 필드 신설 +
   GameSession.CheckPartyWaveState()가 그 자리에서 바로 넘겨주기 + UnitFunction.CastRay의
   오브젝트 발견 지점에서 Tags.Contains("WipeoutTrace")면 OnWipeoutTraceReflected(TraceId) 호출로
   마감. 13-2장이 이제 처음부터 끝까지(전멸→흔적 등록→스폰→발견→전역 위험도 반영, 동일ID당
   1회 제한까지) 완전 자동이다.

2. [완료, 2026-07-09] 16장 "타일 최종 흥미도 = 기본 탐사 흥미도 + 오브젝트 흥미도 + 유닛 흥미도"
   중 "유닛 흥미도" 항 — PersonalMapKnowledge에 이미 있던 _monsterSightings(CastRay가 몬스터를
   목격할 때마다 타일+흥미도 스냅샷을 저장하던 구조, 18장 개인 흥미도 조회에 이미 쓰이고 있었음)를
   그대로 재사용했다. GetUnitInterestAtTile(pos) 신설(오브젝트 쪽 GetObjectIdAtTile과 동일하게
   선형 탐색) → GetTileInterest()가 objectInterest + unitInterest를 더해 ComposeTileInterest에
   넘긴다(함수 시그니처는 2항 그대로 둬서 기존 테스트(5+120=125)를 안 건드림). 별도의 "유닛 위치
   그리드 연동" 배선이 필요 없었다 — 이미 그 목적으로 쓰이던 데이터가 있었다. (1) 개정판 미반영
   항목은 이제 없음.

3. [완료, 2026-07-09] 오브젝트 "조사(investigate)" 단계 도입 — 사용자 확인 결과 "자동 순차 처리"
   (플레이어 조작은 지금처럼 1회 상호작용 그대로, 내부적으로만 조사→회수/확인을 순서대로 자동
   처리)로 결정. InteractableObject.IsInvestigated 필드 신설. Action_PlayerCommandExecute.
   Execute()가 오브젝트에 도달하면: Loot 태그(일반 루팅)면 먼저 OnObjectInvestigated(50%감소,
   17장)를 호출하고 IsInvestigated=true로 표시한 뒤, 이어서 항상 CollectObject+OnObjectCollected
   (0%, 회수/확인 완료)를 호출한다. Corpse/WipeoutTrace 태그는 문서상 조사 단계가 없으므로
   (19장: "확인하면 원인 확인 여부와 관계없이 흥미도를 제거") 조사를 건너뛰고 바로 0%로 간다.
   E_OBJECT_INVESTIGATED(17장), 19장 "확인 시 흔적 흥미도 0"이 자동 발동으로 이동. E_CORPSE_TRACE_
   CHECK의 이해도/위험도 가산분(3-1장)은 대상 Unit이 필요한 HumanKnowledgeBase.RecordEvent 파이프
   라인에 태울 방법이 없어(시체는 죽은 유닛의 잔재라 살아있는 target Unit이 없음) 여전히 미연결—
   "흥미도 0" 부분만 이번에 풀렸다. (2026-07-20 재검증: grep으로 재확인, 여전히 호출부 0곳.)

4. [설계 필요, 반복적으로 나오는 큰 블로커] 소리/전파(간접 파악 InfoType.Indirect) 시스템
   3-1장의 INDIRECT 계열 이벤트 3종(E_HUMAN_KILL_INDIRECT, E_HIT_HEAVY_INDIRECT,
   E_MONSTER_KILL_INDIRECT) 전부 데이터는 등록돼 있지만 발동 지점이 없다. "시야 밖에서 소리로
   사건을 인지"하는 별도 판정 시스템이 필요해 파티/오브젝트보다 설계 범위가 크다. 6장(생존자 전역
   반영의 5:3:2 비율), 23장(정보 오차 ±10%)이 이미 InfoType.Indirect 분기를 완비해 두고 대기
   중이라 "발동 지점만 만들면 나머지 파이프라인은 이미 있다"는 점에서 여전히 우선순위 후보.
   (참고: 02_인지·정보판정·실패처리 문서 24/25장도 동일하게 08_전파·소리 문서를 기다리는 중 —
   이 시스템이 생기면 두 문서의 블로커가 동시에 풀린다.)

5. [더 큰 스코프] 버프/디버프/소환 시스템 — E_STATUS_SELF/SEEN은 스턴에서만 부분 작동
   (ApplySlow/Poison/Burn 호출부 없음), E_DEBUFF_*/E_BUFF_SEEN/E_SUMMON_SEEN은 대응 개념 자체가
   코드에 없음. 함정/방어건물 엔티티(E_TRAP_*/E_DEFENSE_DISABLE_*)도 동일하게 백지 상태 —
   전투 시스템 쪽 설계가 먼저 필요해 이번 우선순위 목록에서는 후순위.

6. [대기, 우리 쪽에서 진행 불가] 13-1장 파티 전멸 "레벨 총합 보정값" — Unit.level/exp/killCount
   필드는 있지만 문서가 정확한 보정 공식을 아직 안 줌("레벨 구조가 정리되면 추가한다"고만 명시).
   문서에 공식이 추가되면 Party.Members의 level 합산 후 그 공식만 얹으면 되는 상태로 대기.

7. [부분 완료, 2026-07-09] 24장 위치/상태 기록 충돌 처리 — 문서가 나열한 9개 대상 중 지금 실제
   데이터 경로가 있는 건 "유닛 마지막 확인 위치"(PersonalMapKnowledge._monsterSightings) 하나뿐이라
   여기에만 PriorityRank()/ShouldReplace()를 실제로 연결했다. ObserveMonster()에 InfoType 인자
   (기본값 DirectWitness, 하위호환 유지)를 추가하고, 기록마다 RecordedInfoType을 같이 저장해서
   새 정보가 들어올 때마다 우선순위를 비교 — 우선순위가 더 낮은 정보는 기존 기록을 덮어쓰지 못한다
   (24장 1~3번 규칙, WeightSystemTests.cs에 3개 테스트로 고정: 직접경험>나중의직접목격, 같은유형은
   최신우선, 직접목격>간접파악). 지금은 CastRay가 항상 DirectWitness로만 호출해서 매번 "같은 유형
   중 최신"으로 항상 교체되므로(기존 동작과 동일) 실질적 게임플레이 변화는 없다 — #4(소리/전파)가
   생겨 Indirect 소스가 이 기록을 갱신하려 들 때부터 이 우선순위 게이트가 진짜로 작동한다.
   나머지 8개 대상(오브젝트 위치, 함정/방어건물 상태, 문/통로 상태 등)은 여전히 미연결 — 상당수는
   대응 엔티티가 아예 없고(함정/방어건물/문), 오브젝트 위치/방 탐사 상태/타일 확인 상태 등 엔티티가
   있는 것들도 지금은 전부 DirectWitness 단일 소스만 써서(IsObjectKnown이 재등록 자체를 막음 등)
   실제로 경합할 두 번째 정보원이 없다 — 이것도 근본적으로 #4에 막혀 있다.

════════════════════════════════════════════════════════════════════════
파티 시스템 (2026-07-08 신규, 샘플 구현 — 5-2/6/13/23장이 전제하는 "파티" 단위의 실체)
════════════════════════════════════════════════════════════════════════
새 파일: Assets/Script/Unit/Party/Party.cs(Id/Name/Members/WaveMonsters/WaveEnded, IsWiped/
IsWaveCleared/GetSurvivors 판정). Human.party 역참조 필드(Unit.cs). GameSession.parties 목록 +
CreateParty(name, members) + CheckPartyWaveState(deadUnit)(RemoveDeadUnit에서 유닛이 죽을 때마다
호출 — 파티원이면 전멸 여부, 몬스터면 그 몬스터가 속한 웨이브의 클리어 여부 확인). WaveData에
partyName/partyUnits 필드 추가, WaveSpawner.SpawnWave()가 몬스터 웨이브와 함께 인류 파티를 시작방
(StartRoom)에 스폰하고 CreateParty로 등록 + party.WaveMonsters를 그 웨이브의 몬스터로 채운다.

이 샘플이 실제로 트리거하는 것: 13-1장 파티 전멸(OnPartyWipeout), 13-2장 전멸 흔적 등록
(RegisterWipeoutTrace) + 07-09부터는 전역 반영(OnWipeoutTraceReflected)까지, 6장 웨이브 클리어 시
생존자 전역 반영(OnWaveEnd) — 전부 문서 그대로, Party.WaveEnded 플래그로 파티당 정확히 1회만.
23장 파티 입장 시 정보 오차 공유는 캐싱 API(HumanKnowledgeBase.ApplyHiddenInfoNoiseForParty/
ApplyInfoValueNoiseForParty)로 구현했지만 원본 노이즈 함수 자체에 아직 실제 호출부가 없어(9장/
CastRay 미노이즈 기록) 대기 상태. (2026-07-20 재검증: grep으로 재확인, 여전히 호출부 0곳.)

의도적으로 안 한 것: 5-2장(같은 Party 인스턴스가 여러 웨이브에 걸쳐 재입장하며 기억을 유지하는
흐름), 13-3장(전멸 전투 스킬 기록 시스템 없음) — 이번 샘플 범위 밖으로 명시적으로 뒀다. 자세한
판단 근거는 아래 해당 장 항목. (2026-07-20 재검증: `new Party(` grep 1곳뿐 — WaveSpawner가 매
웨이브 새 Party를 만드는 구조 그대로 확인.)

════════════════════════════════════════════════════════════════════════
시체/전멸 흔적 엔티티 (2026-07-09 신규 — "시체 깡통" 커밋)
════════════════════════════════════════════════════════════════════════
InteractableObject에 Tags(List<string>)/CauserStage(DangerStage)/TraceId(string, 07-09 추가) 필드가
생겨 기존 "루팅 오브젝트" 하나로 뭉뚱그려 있던 개념이 태그로 분화됐다.
  - GameSession.RemoveDeadUnit()이 유닛이 죽을 때마다 자동으로 Tags=["Corpse", Human/Monster]
    오브젝트를 그 위치에 스폰(어두운 붉은색 시각화). 원인 대상(lastAttacker)의 위험도 단계도 함께
    기록된다.
  - GameSession.CheckPartyWaveState()가 파티 전멸 시 Tags=["WipeoutTrace"] 오브젝트를 함께 스폰
    (검은색 시각화), RegisterWipeoutTrace()가 발급한 traceId를 이제 그대로 실어서 넘긴다(07-09
    직접 연결).
  - UnitFunction.CastRay의 오브젝트 발견 지점(PersonalMapKnowledge.RegisterObject 호출부)에서
    태그를 보고 분기: Corpse면 WeightMath.CorpseTraceInterest(causerStage)(19장 공식, 기본10+
    단계보정)를, WipeoutTrace면 WeightMath.WipeoutTraceBaseInterest를 흥미도로 즉시 적용한다.
    WipeoutTrace 발견 시 OnWipeoutTraceReflected(TraceId)까지 함께 호출하도록 연결해서, 13-2장
    "생환 파티가 흔적을 발견 → 동일 ID당 1회만 던전 위험도 반영"이 완전 자동이다.
    🔵 07-20 재검증: WipeoutTrace 쪽은 위험도 단계 보정이 없어 시체와 비대칭 — 위 "새로 발견한 것
    2번"/19장 항목 참고, 가중치_수정예정_2026-07-20.txt 항목 1로 등록.
  - 07-09(2차): Action_PlayerCommandExecute가 오브젝트 도달 시 Corpse/WipeoutTrace는 조사 단계
    없이 바로 OnObjectCollected()로 흥미도 0 처리(19장 "확인" 규칙), Loot는 조사(50%)→회수(0%)를
    자동 순차 처리하도록 연결됐다(InteractableObject.IsInvestigated 신설, 위 "다음 우선순위 3번").
    E_CORPSE_TRACE_CHECK의 이해도/위험도 가산분만 대상 Unit 부재로 여전히 미연결.
  - 참고: [EventId:...] 콘솔 로그는 너무 시끄러워서(07-09, 사용자 요청) 전부 제거했다 — "발동
    여부"는 이제 로그가 아니라 아래 장별 항목의 실제 호출부 유무로 판단한다.
  - 🔵 07-20 재검증: 시체/전멸흔적/루팅 태그 검사가 InteractableObject.Tags(계층형 문자열,
    "Object/Passable/Corpse" 등)에 대해 정확 일치(Contains(string))로 돼 있던 잠재 버그를 같은 날
    시야-인지 세션에서 발견해 수정했다(RegisterObject/Actions.cs/CastRay 3곳, 부분 일치로 교체) —
    이 버그가 있던 동안엔 시체 흥미도 위험도단계 보정(10+보정)이 한 번도 적용 안 되고 매번 기본
    baseInterest(생성자 파라미터, 결과적으로 10)로만 떨어졌을 가능성이 높다. 지금은 정상 작동.

════════════════════════════════════════════════════════════════════════
1장. 가중치 관리 범위
════════════════════════════════════════════════════════════════════════
✅ 이해도 0~100                — WeightMath.UnderstandingMin/Max, Clamp(WeightType.Understanding)
✅ 개별 유닛 위험도 0~999       — WeightMath.DangerMin/Max
✅ 종별 위험도 0~999           — 위와 동일 Clamp 재사용 (SpeciesWeightState.DangerAccumulatedStored)
✅ 타일 위험도 0~999           — PersonalMapKnowledge._tileDanger, GetTileDanger()가 사실상 0~999
   범위 안에서만 값이 들어옴(2~999, 몬스터 기록 위험도 자체가 이미 클램프된 값)
✅ 오브젝트 위험도 0~999       — InteractableObject.BaseDanger, 15장 합성식에서 클램프 적용
✅ 타일 흥미도 0~999           — WeightMath.ComposeTileInterest()의 Mathf.Clamp(InterestMin,Max)
✅ 오브젝트 흥미도 0~999       — PersonalMapKnowledge._objectInterest, WeightMath.InterestMin/Max
✅ 유닛 흥미도 0~999           — HumanKnowledgeBase.GetUnitInterest() 결과에 Clamp(Interest) 적용
✅ 던전 전체 위험도 999 이상 누적 가능 — HumanKnowledgeBase.GetDungeonDanger()는 클램프 없이 합산
✅ 던전 전체 흥미도 999 이상 누적 가능 — GetDungeonInterest()(현재 0 고정, 22장 참고)
✅ 방 위험도 999 이상 누적 가능      — PersonalMapKnowledge.GetRoomDanger()는 클램프 없이 합산
✅ 방 흥미도 999 이상 누적 가능      — GetRoomInterest() 동일
✅ 소수점 저장 + 정수 적용값(내림)   — WeightMath.AppliedValue()=Mathf.FloorToInt, PersonalWeightRecord.
   AppliedValue 프로퍼티도 동일 규칙. 예시(2.7→2, 3.0→3) 별도 테스트는 없지만 FloorToInt로 자명.
   (2026-07-20 재검증: FloorToInt(2.7)=2, FloorToInt(3.0)=3 직접 계산으로 재확인.)

════════════════════════════════════════════════════════════════════════
2장. 정보 유형 배율 기준값
════════════════════════════════════════════════════════════════════════
✅ 직접경험 1.0 / 직접목격 0.5 / 간접파악 0.25 — WeightMath.cs 주석에 명시, "3장 표 값을 그대로
   쓰고 다시 곱하지 않는다"는 문서 지시를 그대로 따름(실제 연산에 곱해지는 코드 없음 = 의도대로).
   ⚠️ 주의: WeightMath.RatioValue()는 이 배율이 아니라 6-1장 비율값(5/3/2)이다 — 이름이 비슷해
   혼동하기 쉬우니 그대로 재확인.

════════════════════════════════════════════════════════════════════════
3-1장. 이해도/위험도 이벤트 변화량 (weight_events.json, 24행 등록)
════════════════════════════════════════════════════════════════════════
아래 각 행은 "데이터 등록 여부(json)" / "자동 발동 여부(실제 호출부)" 두 가지를 따로 표기.
(2026-07-20 재검증: weight_events.json 24행을 문서 3-1장 표와 한 줄씩 전부 대조 — 수치 전부 정확.)
✅데이터 🔶발동 E_HUMAN_KILL_SEEN(0,+3)        — GameSession.RecordKillWeightEvent, 처치 목격 근사
✅데이터 ❌발동  E_HUMAN_KILL_INDIRECT(0,+1.5) — 소리/전파 시스템 없음
✅데이터 ✅발동 E_HIT_HEAVY_SELF(+0.1,+0.3)    — UnitFunction.TakePhysicalDamage/TakeMagicalDamage
   + ApplyDirectDamage(근접 위협/반응 경로) + DefenseSystem.Block, heavyHitThreshold 이상만.
   🔵 07-20: 이제 RecordHitWeightEvent 안에서 공격자 정체 인지 판정(IsAttackerIdentified, 02문서
   17장)까지 통과해야 발동한다 — 위 "새로 확인/발견한 것 1번" 참고.
✅데이터 ✅발동 E_HIT_HEAVY_SEEN(+0.05,+0.15)  — RecordHitWeightEvent→BroadcastWitnessEvent 근사.
   목격자(BroadcastWitnessEvent) 쪽은 정체 게이팅 대상이 아님(기존 근사 그대로 유지, 시야인지반응
   구현현황 문서의 "다음 우선순위 3번" 참고).
✅데이터 ❌발동  E_HIT_HEAVY_INDIRECT(+0.025,+0.075) — 소리/전파 시스템 없음
✅데이터 ✅발동 E_MONSTER_HIT_SELF(+0.025,0)   — 위 TakePhysicalDamage 계열과 동일 지점(이쪽은
   attacker=인류가 자기 공격 대상을 스스로 아는 경우라 게이팅 대상 아님)
✅데이터 ✅발동 E_MONSTER_HIT_SEEN(+0.0125,0)  — BroadcastWitnessEvent 근사
✅데이터 ✅발동 E_MONSTER_KILL_SELF(+1,-0.5)   — GameSession.RemoveDeadUnit→RecordKillWeightEvent
✅데이터 ✅발동 E_MONSTER_KILL_SEEN(+0.5,-0.25) — 동일 지점, 처치자 제외 근사
✅데이터 ❌발동  E_MONSTER_KILL_INDIRECT(+0.25,-0.125) — 소리/전파 시스템 없음
✅데이터 🔶발동 E_STATUS_SELF(+0.2,+0.4)       — UnitFunction.ApplyStun/Slow/Poison/Burn 공통
   호출(RecordStatusWeightEvent)이지만 실제 호출자는 Projectile/SkillAction의 ApplyStun뿐 —
   ApplySlow/Poison/Burn을 부르는 코드가 게임에 아직 없어 사실상 스턴에서만 발동. 🔵 07-20: 이
   경로도 공격자 정체 게이팅(IsCurrentlyIdentified, RecordHitWeightEvent가 같은 피격 시퀀스에서
   이미 굴린 판정을 조회만 함)이 걸린다.
✅데이터 🔶발동 E_STATUS_SEEN(+0.1,+0.2)       — 위와 동일 사유로 스턴에서만 발동
✅데이터 ❌발동  E_DEBUFF_SELF(+0.2,+0.4)      — 디버프 시스템(적용 개념) 자체 없음
✅데이터 ❌발동  E_DEBUFF_SEEN(+0.1,+0.2)      — 동일
✅데이터 ❌발동  E_SUMMON_SEEN(+0.1,+0.3)      — 소환 시스템 없음
✅데이터 ❌발동  E_BUFF_SEEN(+0.1,+0.2)        — 버프 시스템 없음
✅데이터 ❌발동  E_TRAP_HIT_SELF(+0.2,+0.4)    — 함정 엔티티 없음
✅데이터 ❌발동  E_TRAP_HIT_SEEN(+0.1,+0.2)    — 동일
✅데이터 ❌발동  E_TRAP_DISARM_SELF(+0.3,-1)   — 동일
✅데이터 ❌발동  E_TRAP_DISARM_SEEN(+0.15,-0.5) — 동일
✅데이터 ❌발동  E_DEFENSE_DISABLE_SELF(+0.3,-1) — 방어건물 엔티티 없음(DefenseSystem.cs는 유닛
   개인 방어판정이라 무관)
✅데이터 ❌발동  E_DEFENSE_DISABLE_SEEN(+0.15,-0.5) — 동일
✅데이터 🔶발동 E_CORPSE_TRACE_CHECK(+0.1,+0.2) — 시체 엔티티는 생겼지만(위 섹션 참고) "확인(check)"
   단계가 없어(발견되면 바로 회수 가능) 여전히 발동 안 함. (2026-07-20 재검증: grep으로 재확인,
   여전히 호출부 0곳.)
❌데이터 ❌발동  E_PARTY_WIPEOUT(0,던전+30)     — json 표 대신 HumanKnowledgeBase.OnPartyWipeout()
   전용 메서드로 분리 구현(수치는 13-1장에서 재확인). ✅발동 — 파티 시스템으로 자동 발동.
✅데이터 ✅발동 E_WIPEOUT_TRACE(0,25+단계보정) — RegisterWipeoutTrace()/OnWipeoutTraceReflected()
   전부 실제 호출부 연결 완료(위 "시체/전멸 흔적 엔티티" 섹션 참고).
✅데이터 🔶발동 E_WIPEOUT_UNIT_SKILL_SEEN(+1,5+킬수) — json엔 기저값 5만 등록, 킬수 가산은 호출부
   책임(RecordEvent 호출 자체가 없음 — 전멸 전투 기록 시스템 없어 수동 호출만 가능)

════════════════════════════════════════════════════════════════════════
3-2장. 흥미도 이벤트 기본값 (11개, 상태전이형 — WeightEventTable 미사용)
════════════════════════════════════════════════════════════════════════
🔶 E_UNEXPLORED_TILE(미탐사 기본흥미도 사용) — GetTileInterest()가 매 호출 계산은 하지만 "이벤트
   1회 발생"으로 볼 만한 별도 트리거 지점이 없음(연속 재계산이라 상태전이가 아니라 상시 계산값)
✅ E_EXPLORED_SAFE_TILE(흥미도 0) — PersonalMapKnowledge.RevealTile() 최초 호출 시점에 발동.
   🆕(1) 개정: 이제 "최초 호출 즉시 0"이 아니라 확인 시간(16장 신규 표)을 거쳐야 실제로 0이 됨 —
   TickTileInterestConfirm()이 처리, PersonalMapKnowledge.cs:19-54 / UnitFunction.cs:365-370
   (2026-07-20 재검증: 시야-인지 세션에서 이 타이머 자체가 CanPerceive 게이팅을 안 받는다는 걸
   발견 — 시야인지반응_수정예정_2026-07-20.txt 항목 6 참고, 기절 시에도 확인 시간이 계속 흐름.)
✅ E_INTEREST_OBJECT_FOUND(오브젝트 기본위험도/흥미도 사용) — InteractableObject(Assets/Script/
   Map/InteractableObject.cs, GameSession.objectGrid)가 실제 엔티티이고 UnitFunction.CastRay가
   최초 발견 시점에 등록을 트리거한다. 15장 공식을 위해 BaseDanger 필드도 있고(기본 0), 20/21장
   ObserveObjectInRoom도 같은 지점에서 함께 호출된다(그 방의 "확인된 오브젝트" 목록에 반영).
   (2026-07-20: 이 "최초 발견"이 이제 02문서 12장 확률표 기반 정확 인지 판정을 실제로 통과해야
   일어난다 — 예전엔 가시성>0 이분법이었음. 시야인지반응_구현현황_2026-07-20.txt 참고.)
✅ E_OBJECT_INVESTIGATED(50% 감소) — OnObjectInvestigated(), 예시(120→60) 테스트 고정.
   InteractableObject.IsInvestigated 필드 신설 + Action_PlayerCommandExecute가 오브젝트 도달 시
   (Loot 태그에 한해) 조사→회수를 자동 순차 호출하도록 연결(다음 우선순위 3번 참고, 사용자 확인
   결과 "자동 순차 처리"로 결정).
✅ E_OBJECT_COLLECTED(흥미도 제거) — OnObjectCollected() → 0. Actions.cs Action_PlayerCommandExecute
   가 유닛이 상호작용 대상 타일에 도달하면 호출 + 20/21장 방 확인목록에서도 제거하도록 보강
   (ClearObjectFromRooms, 안 지우면 회수된 오브젝트의 옛 스냅샷이 방 위험도/흥미도에 계속 잡힘)
🔶 E_OBJECT_DROPPED_BY_CARRIER_DEATH(기본×0.5 재적용) — OnObjectDroppedByCarrierDeath(),
   WeightMath.ObjectInterestAfterDrop() 공식은 있음. 이 이벤트는 "오브젝트를 들고 이동하다 도중에
   사망"하는 운반 모델을 전제로 하는데, 현재 구현은 도달 즉시 회수(운반 상태 자체가 없음)라 이
   이벤트가 적용될 지점이 아예 없다(억지로 걸 게 아니라 정직하게 미연결로 남김). (2026-07-20
   재검증: grep으로 재확인, 여전히 외부 호출부 0곳.)
🔶 E_OBJECT_DESTROYED(흥미도 0) — OnObjectDestroyed() → 0. 오브젝트를 파괴/소모하는 상호작용이
   없어(회수만 가능) 호출부 없음. (2026-07-20 재검증: grep으로 재확인, 정의 외 호출부 0곳.)
✅ E_CORPSE_TRACE_FOUND(기본흥미도 사용) — WeightMath.CorpseTraceInterest() 공식이 시체 엔티티
   발견 시(UnitFunction.CastRay→RegisterObject) 자동 적용된다. (2026-07-20: 이 특수 공식이 실제로
   적용되려면 시체/전멸흔적 태그 부분일치 수정이 선행돼야 했다 — 위 "시체/전멸 흔적 엔티티" 섹션
   마지막 항목 참고, 오늘 같이 고쳐졌다.)
✅ E_CORPSE_TRACE_CHECK(흥미도 0) — Corpse/WipeoutTrace 태그 오브젝트는 조사 단계를 건너뛰고
   도달 즉시 OnObjectCollected()로 바로 0이 된다(위 우선순위 3번). 🔶 3-1장의 이해도+0.1/위험도+0.2
   가산분은 HumanKnowledgeBase.RecordEvent가 요구하는 살아있는 target Unit이 시체에는 없어(원인
   제공자는 죽었거나 이미 사라졌을 수 있음) 여전히 미연결 — "흥미도 0" 처리만 자동화됨.
✅ E_WIPEOUT_TRACE_FOUND(전멸흔적 흥미도 사용) — WipeoutTraceGlobalReflection() 공식, 전멸 흔적
   오브젝트 발견 시(CastRay) 흥미도 적용. 🔵 07-20: 이건 13-2장(던전 전역 반영값) 공식이고, 19장
   "타일 흥미도" 쪽(_objectInterest)은 아직 보정 없는 고정 25 — 위 "새로 발견한 것 2번" 참고,
   이 둘을 혼동하지 않을 것(이름이 비슷해 헷갈리기 쉬움).
✅ E_WIPEOUT_TRACE_CHECK(흥미도 0, 발견정보 생성) — OnWipeoutTraceReflected()가 CastRay의 전멸
   흔적 발견 지점에서 호출된다(TraceId 연결). "흥미도 0"도 위와 동일하게 도달 시 자동.

════════════════════════════════════════════════════════════════════════
4장. 개인 가중치 계산
════════════════════════════════════════════════════════════════════════
✅ 개인 계산값 = 현재 저장값 + 변화량 — WeightMath.ApplyPersonalChange(), HumanKnowledgeBase.
   RecordEventForWeight()가 즉시 반영
✅ 예시(2.0+3=5.0→적용값5) — WeightSystemTests.PersonalWeightChange_Example

════════════════════════════════════════════════════════════════════════
5장. 신규 진입 유닛 정보
════════════════════════════════════════════════════════════════════════
✅ 5-1. 기억 없는 유닛 = 최신 전역 지도/미표기 정보 상속 — HumanKnowledgeBase.
   InitializeNewUnitPersonalInfo(Unit) 구현됨, 실제 호출부는 GameSession.CreateParty()가 파티원을
   등록할 때마다(=웨이브 입장 시점) 수행한다(2026-07-20 재검증: 호출부 그대로 확인). 이미 개인
   기억(personalWeights)이 있는 유닛은 내부에서 덮어쓰지 않으므로(5-2장) 안전하다.
❌ 5-2. 기억 있는 유닛 우선사용 + 동료 병합 — 파티 시스템은 있지만, 같은 Party 인스턴스가 여러
   웨이브에 걸쳐 재사용되는(=기억을 유지한 채 재입장하는) 흐름은 이번 샘플 범위 밖(WaveSpawner가
   스폰마다 새 Party를 만듦, 2026-07-20 재검증에서도 확인). 병합 세부 로직은 여전히 6장 재사용으로
   대체 판단(별도 구현 없음).

════════════════════════════════════════════════════════════════════════
6장. 생존자 전역 반영
════════════════════════════════════════════════════════════════════════
✅ 6-1. 비율값 5:3:2, 반영비율(반올림 둘째자리) — WeightMath.RatioValue()/ComputeReflectionRatios()/
   RoundRatio(). 예시1(0.6/0.4)·예시2(0.71/0.29)·예시3(1.0) 전부 테스트 고정
✅ 6-2. 동일 수치 정보 중복 제거(EventID+IncidentID+TargetID+WeightType+InfoType+ChangeValue) —
   IncidentEntry.DedupKey, WeightMath.DedupExact()
✅ 6-3. 동일 정보유형 내 수치오차 평균 — WeightMath.RepresentativeChange()(private), 예시
   (3+3.2)/2=3.1 테스트 고정
✅ 6-4. 전역 반영량 = Σ(대표변화량×비율값)/Σ비율값 — ComputeGlobalReflectionAmount(), 예시1(0.21)·
   예시2(2.4) 테스트 고정
✅ 6-5. 처리 순서 1~9단계 — HumanKnowledgeBase.OnWaveEnd(survivors)가 순서대로 수행. Party
   시스템(Assets/Script/Unit/Party/Party.cs) + GameSession.CheckPartyWaveState()가 "파티와 함께
   입장한 웨이브 몬스터가 전부 죽으면 웨이브 클리어"로 판정해 그 시점의 생존 파티원
   (Party.GetSurvivors())으로 OnWaveEnd를 호출한다. "생존/후퇴/도주 성공 유닛"(문서 원문)을 이
   샘플에서는 "웨이브 클리어 시점 생존자"로 근사 — 후퇴/도주를 판정하는 별도 시스템은 아직 없다.
   (2026-07-20 재검증: OnWaveEnd 전체를 다시 읽고 9단계 순서 재확인, 정확.)

════════════════════════════════════════════════════════════════════════
7장 / 7-1장. 이해도 증가 / 특수 유닛 이해도
════════════════════════════════════════════════════════════════════════
✅ 이해도 계산값 = 저장값+변화량, 예시(0.9+0.1=1.0) — 4장과 동일 함수, 테스트 고정
✅ 특수유닛 이해도 = min(종별,50)+min(개별,50) — WeightMath.ComposeUnderstanding(),
   Unit.isSpecialUnit(UnitVisualDefinition에서 Inspector로 설정, ApplyStatsTo가 복사)로 분기

════════════════════════════════════════════════════════════════════════
8장. 이해도 총량 한도 및 감소
════════════════════════════════════════════════════════════════════════
✅ 검증용 한도 200 — WeightMath.ValidationUnderstandingPoolCap
✅ 최종목표 한도 8000 — WeightMath.FinalUnderstandingPoolCap(상수만, 검증용 200이 실제 사용됨 —
   문서가 "검증 단계엔 200"이라 명시했으므로 의도된 상태)
✅ 감소기준 2웨이브 미갱신 / 유지비율 30% — UnderstandingDecayStaleWaves/DecayFloorRatio
✅ 초과량/감소하한/감소가능량/실제감소량 계산 — ComputePoolDecrease(), 예시(초과2·하한6·가능14·
   실제2·결과18) 테스트 고정. (2026-07-20 재검증: 직접 대입해서 재계산 — 199+3-200=2, 20×0.3=6,
   max(0,20-6)=14, min(2,14)=2, 20-2=18. 문서 예시와 정확히 일치.)
🔶 "1순위 미갱신 / 2순위 최대치도달" 중 2순위는 미구현(단순화) — FindStaleUnderstandingTarget()은
   1순위만 처리, 문서가 두 기준의 우선순위를 명시하지 않아 단순화한 것

════════════════════════════════════════════════════════════════════════
9장. 미표기 내부 정보 오차
════════════════════════════════════════════════════════════════════════
✅ 0~9:±50% / 10~19:±40% / 20~39:±30% / 40~59:±20% / 60~69:±10% / 70~79:±6% / 80~99:±5% /
   100:0% — WeightMath.HiddenInfoNoiseRatio() 8구간 전부, 예시(실제100,이해도35,±30%,70~130)
   테스트 고정. 정수 랜덤 치환 — ApplyHiddenInfoNoise()
❌ 근접/원거리 유형 태그 / 스테이터스 추정값 / 약점 태그 / 대응 장비 태그 / 대응 스킬 태그 —
   5개 항목 전부 실제 데이터 필드 없음(도감/장비 시스템 없음). 공식은 구현, 적용 대상이 없음

════════════════════════════════════════════════════════════════════════
10장. 위험도 증가
════════════════════════════════════════════════════════════════════════
✅ 위험도 계산값 = 저장값+변화량, 예시(2.0+0.3=2.3) — 4장과 동일 함수, 테스트 고정

════════════════════════════════════════════════════════════════════════
11장 / 11-1장 / 11-2장. 위험도 감소
════════════════════════════════════════════════════════════════════════
✅ 최저위험도 일반3/함정3/방어건물3/보스·네메시스10 — WeightMath.MinDangerFloor(),
   ApplyDangerDecrease()가 항상 준수
✅ 11-1. 판정값=총피해량-기준피해량, ≤-1 충족 — TotalDamageDecreaseQualifies(), 예시
   (2-12=-10,충족) 테스트 고정. 호출은 가능(ApplyTotalDamageDangerDecreaseCheck) 🔶 자동집계 없음
✅ 11-2. 타격당 -0.01, 전투당 누적상한 -1 — PerHitDecreaseValue/MaxAccumPerBattle,
   PerHitDecreaseQualifies(). 자동 트리거까지 연결 완료 — HumanKnowledgeBase.
   ApplyPerHitDangerDecreaseCheck(monster, rawDamage, actualDamage)가 몬스터 인스턴스별 누적
   감소량을 -1 상한으로 관리("같은 전투" 경계가 없어 몬스터 인스턴스 생존 기간 전체로 근사).
   🔵 07-20: 이 호출도 이제 RecordHitWeightEvent의 공격자 정체 게이팅을 통과해야 일어난다 — 위
   "새로 확인/발견한 것 1번" 참고(계산 자체가 "어떤 몬스터인지 알아야" 성립하므로 타당한 의존성).

════════════════════════════════════════════════════════════════════════
12장 / 12-1장. 위험도 단계 / 최종 위험도
════════════════════════════════════════════════════════════════════════
✅ 0~99:단계0 / 100~299:단계1 / 300~599:단계2 / 600~799:단계3 / 800~999:단계MAX —
   WeightMath.GetDangerStage(), 전 경계값 테스트 고정
✅ 최종위험도 = 기본+종별누적+개별누적 — ComposeFinalDanger(999클램프), HumanKnowledgeBase.
   GetFinalDanger()/GetDangerStage() 🔶 이 조회는 OnWaveEnd 이후에만 갱신되는데(6장), 파티의
   웨이브 클리어 시점에 OnWaveEnd가 실제로 불린다(6-5장 참고) — 실시간 지도 표시(personalMap)는
   즉시반영형인 GetPersonalDanger(personalWeights 기준)를 쓴다. GetFinalDanger/GetUnitInterest는
   "웨이브가 끝나야 갱신되는 진영 전체 관점" 값으로 실제 웨이브 클리어마다 최신화된다.

════════════════════════════════════════════════════════════════════════
13장. 전멸 위험도
════════════════════════════════════════════════════════════════════════
✅ 13-1. 파티 전멸 시 던전위험도 +30 — OnPartyWipeout(). GameSession.CheckPartyWaveState()가
   파티원이 죽을 때마다 그 파티의 IsWiped(전원 hp<=0)를 확인해서, 전멸 확정 시점(파티당 정확히
   1회, Party.WaveEnded 플래그로 중복 방지)에 호출한다.
✅ 13-2. 전멸흔적 2웨이브 유지, 발견시 25+단계보정(0/5/10/15/20), 같은ID당 1회반영 —
   RegisterWipeoutTrace/OnWipeoutTraceReflected(Reflected플래그)/TickWipeoutTraces(OnWaveEnd에서
   자동 호출). "생환 파티가 흔적을 발견"하는 OnWipeoutTraceReflected 쪽도 실제로 호출된다 —
   InteractableObject.TraceId 필드를 신설해 RegisterWipeoutTrace가 발급한 ID를 흔적 오브젝트에
   실어 스폰하고, CastRay의 오브젝트 발견 지점에서 Tags.Contains("WipeoutTrace")면
   OnWipeoutTraceReflected(TraceId)를 호출하도록 연결했다. 13-2장은 문서에 적힌 흐름 그대로
   처음부터 끝까지 자동이다. (2026-07-20 재검증: WipeoutTraceGlobalReflection=25+DangerStageBonus
   공식 재확인 정확 — 19장의 "타일 흥미도" 쪽 고정 25와는 별개 값이니 혼동 주의, 위 3-2장 항목 참고.)
✅ 13-3. 전멸스킬목격 이해도+1/위험도5+킬수 — json에 기저값 등록, 킬수 가산은 호출부 책임
   🔶 "전멸 전투 참여 유닛이 전멸 당시 썼던 스킬"을 기록하는 시스템이 없어 발동 조건 판정 불가
🔶 "레벨 총합 보정값" — Unit.level/exp/killCount 필드는 이미 존재하지만, 문서가 "이후 레벨
   구조가 정리되면 추가 보정값을 더한다"고만 하고 정확한 보정 공식을 안 줘서 구현할 공식 자체가
   없다. 문서에 공식이 추가되면 Party.Members의 level 합산 후 그 공식만 얹으면 됨.

════════════════════════════════════════════════════════════════════════
14장. 특수 행동 누적 상한
════════════════════════════════════════════════════════════════════════
✅ 소환10 / 버프10 / 디버프10 클램프 — SpeciesWeightState.AccumulateSummon/Buff/Debuff(),
   HumanKnowledgeBase.AccumulateSpecialAction() 🔶 버프/디버프/소환 시스템 없어 호출부 없음

════════════════════════════════════════════════════════════════════════
15장. 타일 위험도
════════════════════════════════════════════════════════════════════════
✅ 미탐사 기본위험도 2 — WeightMath.UnexploredTileBaseDanger
✅ 탐사완료+안전확인 기본위험도 0 — WeightMath.ExploredSafeTileBaseDanger
✅ 타일위험도 = 있었던 유닛의 기록위험도 — PersonalMapKnowledge.SetTileDangerFromUnit()
   (ObserveMonster 경유, CastRay에서 실제 발동)
✅(1) 개정 구현완료: "타일 최종 위험도 = 기본 탐사 위험도 + 오브젝트 위험도" 합성식 —
   WeightMath.ComposeTileDanger() 신설(ComposeTileInterest와 동일 구조), InteractableObject에
   BaseDanger 필드 추가(기본 0), PersonalMapKnowledge._objectDanger/_objectBaseDanger 추가,
   GetTileDanger()가 GetObjectIdAtTile()로 그 타일의 오브젝트를 찾아 항상 더한다(확인시간 없이
   즉시 가산 — 16장 오브젝트 흥미도와 동일 구조). 표의 "흥미도"는 오기로 확정(문맥/16장 대칭 구조).
✅ 유닛 사라져도 기록 즉시 안 사라짐 — _tileDanger는 명시적으로 지우기 전까지 유지
✅ 단계별 안전확인시간 0~99:1s/100~299:2s/300~599:3s/600~799:4s/800~999:5s —
   WeightMath.TileSafetyCheckSeconds(), TickTileSafety()가 실제 감소 처리
✅ 안전시간 경과→위험도0 — TickTileSafety() 경과시 _tileDanger.Remove (0으로 죽여두지 않고 삭제)
✅ "미탐사 기본위험도 2"도 즉시 0이 아니라 위 안전확인표를 거침 — RevealTile()이 첫 발견 시
   _tileDanger를 2로 시딩 → TickTileSafety가 정상적으로 감소시킴
🔵 07-20 재검증(갱신): "시야로 확인 중" 실시간 판정이 이제 단순 물리적 점유가 아니라 "정확 인지
   중(personalSpottedEnemies 포함)"으로 더 정밀해졌다(threatPresent 조건에 personalSpottedEnemies.
   Contains(occupant) 추가, UnitFunction.OnUpdate) — 02문서 23장 "미인식이면 안전타일로 인지"와
   이제 정합됨. 다만 오브젝트(함정 등) 쪽은 여전히 다른 성격의 갭이 있음(가중치_수정예정 문서 참고
   교차 노트) — 대응 엔티티가 없어 현재는 무해.
🔵 07-20 재검증: 이 안전확인 타이머(TickTileSafety) 자체가 CanPerceive(기절 등 인지불가 상태)
   게이팅을 안 받는 것도 발견 — 시야인지반응_수정예정_2026-07-20.txt 항목 6.

════════════════════════════════════════════════════════════════════════
16장. 타일 흥미도
════════════════════════════════════════════════════════════════════════
✅ 미탐사 기본흥미도 5 — WeightMath.UnexploredTileBaseInterest
✅ 탐사완료 기본흥미도 0 — WeightMath.ExploredTileBaseInterest
✅(1) 개정 구현완료: "타일 최종 흥미도 = 기본 탐사 흥미도 + 오브젝트, 유닛 흥미도" —
   오브젝트 흥미도 합산은 기존 ✅(ComposeTileInterest), "유닛 흥미도"도 합산된다 —
   PersonalMapKnowledge.GetUnitInterestAtTile()이 CastRay가 이미 기록해 두던
   _monsterSightings(18장 개인 흥미도 조회에 쓰이던 동일 데이터)를 재사용해 그 타일의 목격
   몬스터 흥미도 스냅샷 합을 구하고, GetTileInterest()가 이를 objectInterest에 더해서 넘긴다.
   (1) 개정판 미반영 항목은 이제 없음.
🆕(1) 구현 완료: 흥미도 단계별 확인시간 표(0~99:1s~800~999:5s, 15장과 동일 구간) —
   WeightEnums.InterestStage, WeightMath.GetInterestStage()/TileInterestCheckSeconds(),
   PersonalMapKnowledge._tileInterest/_tileInterestConfirmElapsed/TickTileInterestConfirm(),
   RevealTile()이 최초발견시 흥미도5를 시딩. UnitFunction.OnUpdate:363-370에서 매프레임 호출.
✅ 예시(5+120=125) — 테스트로 고정
✅ 오브젝트 회수 완료시 타일 흥미도 별도처리(오브젝트 흥미도만 0, 타일 기본 탐사흥미도는 무관) —
   OnObjectCollected()가 오브젝트 쪽만 0으로 만들고 _tileInterest(타일 쪽)는 안 건드림 → 문서의
   "타일 기본 탐사 흥미도는 오브젝트 흥미도와 별도로 처리한다" 규칙과 일치

════════════════════════════════════════════════════════════════════════
17장. 오브젝트 흥미도 감소/복구
════════════════════════════════════════════════════════════════════════
✅ 조사완료 50%감소 / 회수완료 0 / 파괴·소모 0 — OnObjectInvestigated/OnObjectCollected/
   OnObjectDestroyed, 예시(120→60) 테스트 고정
✅ 드랍후 = 기본흥미도×0.5 — OnObjectDroppedByCarrierDeath(), ObjectInterestAfterDrop(),
   예시(120×0.5=60) 반영
✅ InteractableObject 엔티티가 실제로 있어 회수(OnObjectCollected)뿐 아니라 조사
   (OnObjectInvestigated, Loot 태그 한정)도 Action_PlayerCommandExecute가 도달 즉시 자동
   순차 호출한다(위 "다음 우선순위 3번"). 🔶 드랍(carrier death)/파괴(destroy)는 그에 대응하는
   게임 상태가 아직 없어(운반 중 사망·파괴 상호작용 자체가 없음) 여전히 수동 호출만 가능 — 3-2장
   항목 참고. 트랩/방어건물도 별도 엔티티가 없어 그쪽 이벤트(3-1장 E_TRAP_*/E_DEFENSE_DISABLE_*)
   는 미연결.

════════════════════════════════════════════════════════════════════════
18장. 유닛 흥미도와 이해도 관계
════════════════════════════════════════════════════════════════════════
✅ 일반유닛 흥미도 = 기본흥미도×(100-이해도)% — WeightMath.UnitInterestFromUnderstanding(),
   예시(100×65%=65) 테스트 고정
✅ IsInterestTarget=true면 감소식 미적용 — Unit.isInterestTarget 플래그, HumanKnowledgeBase.
   GetUnitInterest()/GetPersonalInterest() 둘 다 분기 처리
🔶 units.json으로부터의 일괄 로드 경로(UnitVisualDefinition.LoadDataFromJson, UnitJsonNode)엔
   baseInterest/baseDanger/isSpecialUnit/isInterestTarget/heavyHitThreshold가 없어 JSON 일괄
   가져오기로는 채울 수 없다. 다만 JSON → 유닛 프리팹 생성 원클릭 도구(Assets/Editor/
   JsonToUnitPrefabConverter.cs)는 별도로 units.json의 "weight" 블록을 읽어 이 5개 필드를 자동
   반영하도록 이미 고쳐졌다 — UnitVisualDefinition.LoadDataFromJson()(구형 Context Menu 경로)만
   여전히 이 필드들을 못 읽는다(둘이 서로 다른 코드 경로).

════════════════════════════════════════════════════════════════════════
19장. 시체 / 흔적 흥미도
════════════════════════════════════════════════════════════════════════
✅ 시체/흔적 기본흥미도 10, 전멸흔적 25 — WeightMath.CorpseTraceBaseInterest/WipeoutTraceBaseInterest
✅ 확인 시 흔적 흥미도 0(전멸흔적확인 시 발견정보도 생성) — OnWipeoutTraceReflected()의
   발견정보 생성 부분은 CastRay 발견 시점에, "흥미도 0" 부분은 Action_PlayerCommandExecute가
   Corpse/WipeoutTrace 오브젝트에 도달할 때 OnObjectCollected()로 자동 처리된다(위 "다음
   우선순위 3번"). 둘 다 대상이 있다(엔티티+상호작용 경로 모두 갖춰짐).
🔵 07-20 재검증(신규 발견): "위험도단계별 보정(0/+5/+10/+15/+20)"이 시체/흔적(WeightMath.
   CorpseTraceInterest)엔 정확히 적용되지만, **전멸 흔적은 고정 25만 쓰고 보정을 안 받는다**
   (PersonalMapKnowledge.RegisterObject의 WipeoutTrace 분기). 문서 표 헤더가 "시체, 흔적 흥미도"
   라고만 적혀 있어 전멸 흔적에도 적용되는지 애매했는데, 같은 문서 13-2장이 이미 동일 구조
   (25+보정)로 전멸 흔적에 보정을 적용하고 있어 대칭성 근거로 "적용" 쪽으로 확정(사용자 확인).
   제안 코드는 가중치_수정예정_2026-07-20.txt 항목 1에 작성 완료, 코드는 아직 미반영(다른
   작업자와 PersonalMapKnowledge.cs가 겹칠 수 있어 보류).
✅ 자동 트리거 — 시체/전멸흔적 엔티티가 실제로 생겨서(InteractableObject, 위 섹션 참고) CastRay
   발견 시점에 이 공식이 자동 적용되고, 확인(check) 시 흥미도 0 처리까지 자동이다(19장 위 항목
   참고). 19장은 문서에 적힌 흐름 그대로 처음부터 끝까지 자동(위 전멸흔적 보정 갭 제외).

════════════════════════════════════════════════════════════════════════
20장 / 21장. 방 위험도 / 방 흥미도
════════════════════════════════════════════════════════════════════════
✅ 일반방 미탐사20/보스방70, 탐사중 일반10/보스35 — WeightMath.Unexplored/ExploringRoomBaseDanger 4종
✅ 일반방 미탐사30/보스방50, 탐사중 일반15/보스25(흥미도) — 대응 Interest 상수 4종
✅ 미탐사=기본값 / 탐사중=확인유닛+확인오브젝트+고정값 / 탐사완료=확인유닛+확인오브젝트 —
   PersonalMapKnowledge.GetRoomDanger()/GetRoomInterest(), RoomExploreState 3단계 switch
✅ 예시(유닛650+오브젝트480=1130 / 오브젝트800+유닛350=1150) — 공식 구조상 그대로 성립
✅ 타일 위험도/흥미도는 방 위험도/흥미도에 합산 안 함 — GetRoomDanger/Interest 어디에도 타일
   딕셔너리 참조 없음(서로 다른 데이터 구조)
✅ roomId/isBossRoom 자동 연동 완료 — UnitFunction.CastRay가 Chunks.roomId/Chunks.roomRole
   (==RoomRole.BossRoom)을 그대로 사용해 실시간 연결했다.
   - 타일을 처음 밝힐 때(RevealTile의 isFirstReveal, 바닥 타일만) →
     PersonalMapKnowledge.ObserveRoomTileRevealed(roomId, isBossRoom, totalFloorTiles) 호출.
     Unexplored였다면 Exploring으로 전환하고, 누적 확인 타일 수가 totalFloorTiles(그 방의 실제
     바닥 타일 총수, CreateMap.GetRoomFloorTileCount)에 도달하면 Complete로 전환한다.
   - 몬스터/오브젝트를 목격할 때 → ObserveUnitInRoom/ObserveObjectInRoom도 같은 곳에서 호출.
     대상 키(유닛/오브젝트 인스턴스명)별 최신 스냅샷 Dictionary라 매 프레임 재호출에도 안전.
   - 오브젝트가 회수/파괴되면 OnObjectCollected/OnObjectDestroyed가 방의 확인목록에서도 그
     오브젝트를 제거해 방 위험도/흥미도가 옛 스냅샷으로 부풀지 않게 한다.

════════════════════════════════════════════════════════════════════════
22장. 던전 전체 위험도 / 흥미도
════════════════════════════════════════════════════════════════════════
✅ 던전위험도 = 모든방위험도합산+파티전멸누적+전멸흔적전역반영누적 — HumanKnowledgeBase.
   GetDungeonDanger(Human observer)(= observer.personalMap.GetPersonalDungeonDanger() + 기존
   GetDungeonDanger()). "모든 기록된 방"은 진영 전체 지도가 없어 이 관찰자 개인이 아는 방
   전체로 근사.
✅ 던전흥미도 = 모든방흥미도합산 — GetDungeonInterest(Human observer)
   (= observer.personalMap.GetPersonalDungeonInterest()). 파티전멸/전멸흔적에 대응하는 흥미도
   누적항이 문서에 없어(13장은 위험도만 다룸) 방 합산 1항으로 끝나는 게 맞다.
   인자 없는 기존 GetDungeonDanger()/GetDungeonInterest()는 "진영 차원 누적값만"이 필요한
   호출부를 위해 그대로 남겨둠(하위호환, 시그니처 오버로드).
✅ 탐사완료 방도 0 고정 안 함(남은 유닛/오브젝트/흔적 총합) — GetRoomDanger/Interest의
   RoomExploreState.Complete 분기가 ConfirmedUnit+ConfirmedObject 그대로 반환(0 강제 없음)

════════════════════════════════════════════════════════════════════════
23장. 정보 오차
════════════════════════════════════════════════════════════════════════
✅ 직접경험 0타일/0% — WeightMath.InfoError(DirectExperience,_) = (0,0)
✅ 직접목격 0타일/±5% — InfoError(DirectWitness,_) = (0,0.05)
✅ 간접파악 ±1타일/±10% — InfoError(Indirect,_) = (1,0.10)
✅ 공포단계 ±1타일/±15% — mentalErr(Fear) = (1,0.15), "더 큰 쪽 채택" 규칙으로 병합
✅ 공황단계 ±2타일/±25% — mentalErr(Panic) = (2,0.25)
✅ 예시(실제100,간접파악,±10%,90~110) — WeightMath.ApplyValueNoise(), 테스트 고정
🔶 "전역 정보 오차는 파티 입장 시 1회 산출, 같은 파티 공유" — 캐싱 API는 있다.
   HumanKnowledgeBase.ApplyHiddenInfoNoiseForParty(party, cacheKey, ...)/
   ApplyInfoValueNoiseForParty(party, cacheKey, ...)가 (party.Id + cacheKey) 키로 최초 1회만
   계산하고 이후 같은 파티의 호출은 캐시된 값을 그대로 반환한다(ClearPartyInfoNoiseCache로 재입장
   시 초기화 가능). 다만 원래 노이즈 함수(ApplyHiddenInfoNoise/ApplyInfoValueNoise) 자체를 실제로
   부르는 게임 지점이 아직 없어서(9장 내부정보 필드 없음, CastRay의 목격 기록은 노이즈 없이
   정확한 값을 그대로 저장) 이 파티 캐싱 API도 당장은 호출부가 없다. (2026-07-20 재검증: grep으로
   재확인, 4개 메서드 전부 외부 호출부 0곳.)

════════════════════════════════════════════════════════════════════════
24장. 위치 및 상태 기록 충돌 처리 (🆕(1) 개정 — 원본은 "정보 우선순위와 갱신")
════════════════════════════════════════════════════════════════════════
✅ 우선순위 1~5(최신직접경험>직접경험>직접목격>간접파악>오래된간접파악) — WeightMath.
   PriorityRank(), 숫자가 작을수록 우선
✅ 교체 판정(우선순위 높으면 교체/같으면 최신채택/낮으면 유지) — WeightMath.ShouldReplace()
🆕(1) 부분구현: 적용 대상 명시 목록 9개(오브젝트 위치기록/유닛 최종확인위치/함정·
   방어건물 상태/오브젝트 회수·드랍·파괴상태/방 탐사상태/타일 확인상태/시체·흔적 확인상태/전멸흔적
   유지·제거 상태/문·통로·장애물 상태) 중 "유닛 최종확인위치" 1개만 실제로 연결됐다 —
   PersonalMapKnowledge.ObserveMonster()가 InfoType별 PriorityRank/ShouldReplace로 기존 기록을
   교체할지 판단한다(위 "다음 우선순위 7번" 참고, WeightSystemTests.cs에 3개 테스트로 고정). 나머지
   8개는 대응 엔티티가 없거나(함정/방어건물/문) 있어도 현재 단일 정보원(DirectWitness)만 써서
   경합할 두 번째 소스가 없어(#4 소리/전파 시스템 부재) 여전히 미연결.
✅ "가중치 수치 변화량은 이 장이 아니라 6장 규칙을 따른다"는 명확화 — PriorityRank/ShouldReplace
   는 위험도/이해도/흥미도 계산 경로(RecordEvent/OnWaveEnd)에서 쓰이지 않고 위치/상태 기록
   경로(ObserveMonster)에서만 쓰여 일치.
✅ 동일 IncidentID 동일 수치 중복 1회만 기록 — 6-2장 DedupExact()와 동일 메커니즘 재사용 가능

════════════════════════════════════════════════════════════════════════
25장. 공황 단계 정보 평균 처리
════════════════════════════════════════════════════════════════════════
✅ 공황0.3 + 다음우선순위0.7 — WeightMath.PanicWeight/NonPanicWeight, BlendPanicValue()
✅ 예시(140×0.3+100×0.7=112) — 테스트 고정
✅ 6장 파이프라인 통합(같은 InfoType 버킷 내 공황/비공황 혼재 시 블렌드) —
   WeightMath.ResolvePanicBlend()(private), OnWaveEnd 경로에서 자동 적용
🔶 "공황 단계 유닛만 생환 시 단독 확정값 사용"을 수치 병합에도 동일 적용 — 문서가 위치정보
   규칙으로만 명시한 걸 수치 병합에도 확장 해석한 것(애매해서 이렇게 구현, 기존 판단 유지)

════════════════════════════════════════════════════════════════════════
전체 집계 (문서 항목 단위, 대략치 — 2026-07-20 재검증으로 갱신)
════════════════════════════════════════════════════════════════════════
- ✅ 완전 구현(공식/수치가 코드에 있고 조회 가능): 약 140개 항목 그대로 유지.
- 🔶 부분구현/수치상으로만 존재: 약 16개 항목 그대로 — "드랍/파괴/트랩/방어건물/버프·디버프·소환/
  전멸전투기록/소리·전파" 등 대응 게임 상태·엔티티가 없어서다.
- ❌ 순수 미구현: 9장 내부정보 5필드, 13-1장 레벨총합보정(공식 미정) — 2건, 그대로.
- 신규 발견 1건(19장 전멸흔적 보정 누락, 가중치_수정예정_2026-07-20.txt 항목 1) — 코드 미반영 상태로
  다음 우선순위 0번에 추가.
- 계산 공식 자체의 정확도는 매우 높다(문서 예시가 있는 곳은 전부 테스트로 고정돼 있고, 이번
  재검증에서 그 예시들을 직접 재계산해 재확인했다). 남은 미구현/부분구현은 거의 대부분 "그 이벤트를
  발생시킬 하위 게임 시스템이 아직 없다"는 동일한 원인으로 수렴한다 — 특히 #4(소리/전파, 간접 파악
  InfoType.Indirect의 유일한 발동 경로)가 24장 나머지 8개 대상을 포함해 가장 많은 잔여 항목의 공통
  블로커다. 02_인지·정보판정·실패처리 문서 24/25장도 동일한 08_전파·소리 문서를 기다리고 있어, 그
  문서가 생기면 두 시스템의 블로커가 동시에 풀린다.
- (1) 개정판 미반영 항목은 없음(16장 "+유닛 흥미도" 합산이 마지막 남은 항목이었고 이미 풀렸다).
